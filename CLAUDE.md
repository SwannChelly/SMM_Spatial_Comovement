# SMM Spatial Comovement — Codebase Guide

## Changelog

| Date | Files | Summary |
|------|-------|---------|
| 2026-08-03 | `load_parameters.jl`, `optimizer.jl`, `test/test_granular_diagnose.jl`, `CLAUDE.md` | **α's search box gains an ABSOLUTE ceiling: new `ALPHA_MAX = 2.0`, applied in levels on top of the multiplicative box.** The α bound was `[×BOUND_LO, ×BOUND_HI] × TAU_PRIOR` = `[0.1, 10] ×` the prior, so with a prior of ~0.21–0.39 the upper end reached 2.1–3.9 — well outside the meaningful range for a distance elasticity, and a region where `invert_T_ge` has to push remote-origin T very hard to hold γ_ls fixed. The upper bound is now `min(BOUND_HI × anchor, ALPHA_MAX)`. **`BOUND_LO` is left multiplicative** — there is no comparable floor to impose, and α → 0 is a live outcome under profiling. **T is untouched**: it keeps the full `[×BOUND_LO, ×BOUND_HI] × T_rs_init` box, and `plot_T_vs_initial`'s window lines are unaffected. Applied at all THREE sites through one `alpha_box(anchor)` closure in `train_stage`, so they cannot drift apart: the fresh-start bounds, the annealed per-stage radius in continue stages (clamped to the capped box, not the raw one), and the Stage-0 coarse α search (whose range must stay inside the Stage-1 box or the selected `init_alpha` is immediately clamped). Two degeneracy guards: `alpha_box` holds the floor below half the ceiling so the box can never collapse to a point, and a continue stage whose incumbent sits ABOVE the cap — possible when resuming a run whose artefacts predate it — would otherwise get `lb > ub`, so those entries fall back to the full capped box with a warning. `test_granular_diagnose.jl`'s box report mirrors the same formula. |
| 2026-08-03 | `model_CP.jl`, `main.jl`, `main_gmm.jl`, `test/test_cloglog_streaming.jl` (new), `test/test_speed_refactor.jl`, `test/files.md`, `CLAUDE.md` | **Memory: the extensive-margin regression no longer materialises its design, and three full-size matrices per worker are gone. ~240 MB → ~60 MB per worker during PSO; ~1.34 GB → ~300 MB during inference.** At `n_good = 1161`, `N_rho = 723` a run with 49 workers hit the machine's memory limit. Distributed workers are separate PROCESSES, so peak RAM is `n_workers × per-worker working set` — and the per-worker set was dominated by an object with almost no information in it. **(1) The cloglog design was `n_good × N_rho = 839,403` rows, but only `n_good` distinct rows of geography.** Within a good, the bin dummies, the FE label and the weight are all constant — stored 723 times over — and `row_g`/`row_rho` stored an index that is pure arithmetic. New `RegCells` holds each of them ONCE (`gcol`/`gval` as a (column, value) pair, so the `N_REG == 1` continuous-log-distance branch and the `N_REG > 1` dummy branch share one representation; `grp` compacted to `1:n_grp`; `has_z`/`lzc`), i.e. kilobytes against `CloglogDesign`'s 74 MB (408 MB at `N_RHO_INFERENCE`). New `_cloglog_irls_cells` fits from it by streaming accumulation, using the closed form of the weighted-within transform — `X̃'WX̃ = Σ_i W_i X_iX_i' − Σ_p S^X_p (S^X_p)'/S^W_p`, likewise for the rhs, with the fixed effects recovered as `a_p = (S^z_p − S^X_p·β)/S^W_p` — so the `k × k` normal equations are built in ONE pass and the dense kernel's `copyto!(Xt, Xf)` + `W .* Xt`, ~67 MB of churn PER IRLS ITERATION (372 MB at inference) over up to 50 iterations, are gone. **(2) The linear predictor is a per-CELL vector.** `log z[ρ,g] = log(scale_g) + (−1/θ)·log(−log(1−u[ρ,g]))` and the second term is **θ-INVARIANT**, so `η[c,ρ] = Acell[c] + β_size·logz_resid[ρ,c]`: the IRLS state is length `n_cell`, not `n_cell·N_rho`, and every per-row quantity (`η`, `μ`, `dμ/dη`, working response, IRLS weight — eight full-length vectors in the dense kernel) is formed in registers and never stored. `solve_network` now fills `logz_const`; the θ-invariant half is cached by `logz_resid` on the IDENTITY of the draw set, so it is built once per draw set instead of once per evaluation (under the Jacobian, one replication runs `2·n_perturb+1` evaluations on the same `u_k`) — which also removes 839k `log()` calls per evaluation. **(3) Three matrices deleted.** `SAMPLE_WEIGHTS` was `fill(1/N_rho, N_rho, n_good)` — a full-size matrix holding one repeated number, allocated per worker and again per Jacobian/Σ_sim replication; new `FlatWeights <: AbstractMatrix{Float64}` keeps the `[rho, g]` indexing contract with three words of storage (`flat_weight` lets the hot loop hoist it to a scalar). `linkages_flat` is a `BitMatrix` (6.7 MB → 0.1 MB; 37 MB → 0.6 MB) — every consumer reads it through `> 0`/`!= 0`/`sum`/`count`. `z_flat` is no longer returned when the streaming path applies (new `return_z` kwarg on `solve_network`, decided in `SMM`), so it is collectable BEFORE the regression — the peak of the evaluation — instead of being pinned until the moments return. **(4) `addprocs(...; exeflags=--heap-size-hint=…)`** (default 2G, `WORKER_HEAP_HINT=""` to disable) in both entry points: each worker sized its GC threshold against TOTAL system memory with no knowledge of its 48 siblings doing the same, so all of them sat on their garbage at once. This run is churn-dominated, so capping the heap is on its own a large cut in RSS. **(5) The LPM sibling got the same treatment.** `fast_weighted_regression` (`--reg=lpm`) shares the `RegCells` design and fits through new `_wls_cells` — the same closed-form within transform, one pass, no iteration — so the memory profile no longer depends on which link is selected. It solves the NORMAL equations where the dense kernel took a pivoted QR of `sqrt(w).*X`: algebraically identical, and on a 0/1-dummy design the conditioning cost is immaterial (measured 1e-15 against both the dense kernel and an independent explicit-dummy OLS). **(6) `z_flat` is no longer MATERIALISED** when nothing downstream reads it — `solve_network` fills `z_inv_flat` (which drives the Ricardian argmin) column by column through a length-`N_rho` scratch buffer instead of building a second full-size matrix. Same operations on the same values in the same order, so `z_inv_flat` is BIT-IDENTICAL; a last-ulp difference there could flip a winner. **Rank-deficiency now reports itself:** both streaming kernels route their `k × k` solve through `_solve_within_normal_eq`, which replaces a bare `SingularException(k)` with the diagnosis — the usual cause is a bin scheme with no BASE category (`distance_bin` returning 0 for no (r,dr) pair), which makes the dummies sum to the constant the fixed effect already absorbs, or a bin that does not vary within a fixed-effect group. The dense cloglog kernel fails the same way; the dense LPM kernel's tall QR could quietly return a solver-dependent answer instead, so erroring is deliberate. **Escape hatch:** `REG_STREAMING[] = false` routes BOTH links back through their dense kernels, which are retained unchanged as the references (and the cloglog one is still what `test_cloglog_verify.jl` gates against GLM.jl). **NOT bit-identical**, by construction and only through the regression: the streaming accumulation reorders sums, and `logz_const + logz_resid` agrees with `log(z_flat)` to the last ulp of the input rather than bitwise. Measured in an out-of-Julia replication of both kernels: max|Δβ| = 7e-15 across the five design shapes, and 2e-16 for the log-z reconstruction alone (the two `log z` routes differ by 9e-16 absolute) — orders of magnitude below the `tol = 1e-9` the fit itself converges to. **Verified**: the algebra was cross-checked outside Julia (streaming vs dense vs an explicit-dummy cloglog MLE, agreeing to 7e-15 across all five design shapes); new `test/test_cloglog_streaming.jl` is the in-repo gate (streaming vs dense vs GLM.jl, six cases, plus a full-`fast_cloglog_regression` A/B and an allocation report). **Untested here** (no local Julia) — run `julia test/test_cloglog_streaming.jl` first, then `test/test_cloglog_verify.jl` and `test/test_speed_refactor.jl`. `test/test_speed_refactor.jl` never defined `input_folder`/`output_folder` (pre-existing; it errored on include before running a check) — added, with a repo-root check. |
| 2026-08-02 | `tools.jl`, `CLAUDE.md` | **A clean division of labour between the two inference reports: `inference_summary.txt` = the NAIVE sandwich throughout; `inference_delta.txt` = the STRUCTURAL propagation — and `N̂_s` now gets the delta method T already had.** **(1) `inference_summary.txt` is one method.** The extra block added the day before reported only `√diag((G'WG)^{-1})` per parameter. It now stacks the α, T and `N_s` first-difference Jacobian columns into ONE `G` (new `fd_G` kwarg replacing `fd_se`) and applies the file's own machinery — `(G'WG)^{-1}` AND the sandwich `V G'WΩWG V` — so the block reports `se_eff`, `se_sw`, ratio, t and CI, and the number is their JOINT naive variance with α and T treated as free parameters. New artefacts `se_theta_naive_fd{,_efficient}.npy`; the SEs are returned in the result Dict (`se_fd_sw`/`se_fd_eff`/`fd_labels`) so the delta report can contrast against what the summary actually prints. **(2) `N̂_s` is CALIBRATED, not estimated, so it takes a delta method.** It solves `Ḡ_s(n; q̂) = Ĝ_s` with `Ḡ_s(n;q̂) = mean_l (1−q̂_ls)^n`, and `q̂ = q̂(α̂, T*(α̂,γ̂))`. The implicit function theorem gives `dN_s = [dĜ_s − a_s dα̂ − b_s dγ̂]/D_s`, with `D_s = ∂Ḡ_s/∂n = mean_l (1−q̂)^{N̂} ln(1−q̂) < 0`, `a_s = ∂Ḡ_s/∂α` (the TOTAL derivative — it is literally the block-6 row of the profiled α Jacobian `G_alpha`), and `b_s = ∂Ḡ_s/∂γ = (∂Ḡ_s/∂T)·f_γ` (the block-6 rows of the FREE-parameter Jacobian times the `∂T*/∂γ` already computed for T) — **so no new simulation is needed**. `Var(N̂) = L·JointCov(Ĝ, α̂, γ̂)·L'` with `L = [D⁻¹, −D⁻¹A, −D⁻¹B]`, PSD by construction, every block already available: `Σ_ĜĜ = Sigma_data[G,G]`, `Cov(α̂,Ĝ) = P·Sigma_data[:,G]` with the same influence `P` used for `Cov(α̂,γ̂)`, `Σ_γĜ = Sigma_data[γ,G]`. Reported with its three marginal channels (`se_N_target`/`se_N_alpha`/`se_N_gamma`) beside the naive SE, plus a `D_s` table contrasting the exact derivative with the unit first difference `Ḡ(n+1)−Ḡ(n)` (they agree to `O(q̂)`; measured gap 5–7% on the synthetic set). New artefacts `var_theta_N_s_delta.npy`, `se_theta_N_s_delta.npy`, `jacobian_N_s_wrt_{alpha,gamma}.npy`, `dGbar_dn.npy`. **(3) The system is TRIANGULAR — `N_s` noise does NOT affect T.** `α̂ → T̂ = T*(α̂,γ̂) → q̂ → N̂_s`: `invert_T_ge` is the CONTINUUM inversion (`profiling.jl` contains no `N_s` reference at all) and `q̂` is `N_s`-free by Lemma 2, so nothing upstream of `N̂_s` sees it. The arrow runs the other way, and that transmission is exactly what the new `Var(N̂)` captures — on the synthetic set the γ channel DOMINATES (`se_N` 10.5 of which 9.4 is γ, against a naive 4.1 marginal), i.e. the naive SE understates `N̂_s`'s uncertainty by more than 2× because it ignores that γ noise moves T, T moves `q̂`, and `q̂` moves the bisection. **Verified**: granular profiled path emits all three sections and both blocks; the non-granular profiled path degrades correctly; V0 bit-identical to a contemporaneous HEAD run. |
| 2026-08-01 | `tools.jl`, `main.jl`, `CLAUDE.md` | **SEs for T and `N_s` under the profiled estimator: a first-difference-Jacobian block in `inference_summary.txt`, and `inference_T_delta.txt` → `inference_delta.txt` restructured as one α/T/`N_s` report.** Under profiling the inference Jacobian is α-only, so T (the Sinkhorn image) and `N_s` (profiled by bisection) previously appeared value-only, with no SE anywhere. **(1) New `compute_N_s_jacobian`** computes `∂m/∂N_s` by a **unit first difference** `m(N̂_s+1) − m(N̂_s)` — `N_s` is an INTEGER, so one variety IS the step: no step size to choose, no log step, and the SE comes out in varieties. The two evaluations of a replication share draws, so every moment without an `N_s` term differences to EXACTLY zero; only block 6 has one, and `Ḡ_s(0)` depends on `N_s` alone, so the block is DIAGONAL and all `S` columns come from a single simultaneous bump. That structure is **asserted, not assumed** — a nonzero difference outside block 6 errors out, which is gate V10 (block 4 is `N_s`-free by Prop. 1) enforced at runtime on every inference run. **(2) New `_gtwg_inv(G,W)`** factors out `compute_smm_inference`'s Cholesky→eigenvalue-floor PD guard. `Var(N̂_s) = (G_N' W G_N)^{-1}` — the efficient matrix, α's formula, per the design that `N_s` is not calibrated inside the model the way T is; it is the MARGINAL efficient variance holding α̂ fixed, so α's reported SE is unchanged. **(3) `inference_summary.txt`** gains a `--- First-difference-Jacobian SEs (T, N_s) ---` section (new `fd_labels`/`fd_values`/`fd_se`/`fd_title` kwargs on `compute_smm_inference`, defaulting to `nothing` ⇒ byte-identical when unused): T's SE from the FREE-parameter FD Jacobian `√diag((G'WG)^{-1})` over the α+T columns (`main.jl` now passes `J_gb`/`J2_gb` as the new `J_free_gb` kwarg — the full Jacobian it already computes for diagnostics), and `N_s`'s efficient SE. **(4) `inference_T_delta.txt` renamed `inference_delta.txt`** and restructured into three sections whose first column is `parameters`: **α** (reproduced verbatim from `inference_summary.txt` — estimate, `se_eff`, `se_sw`, t, CI), **T** (the same delta-method table plus new `t_α`/`CI_lo_α`/`CI_hi_α` columns — the statistics from the α CHANNEL ALONE, i.e. T's precision if the γ target were known exactly; also saved as `t_stats_T_delta_alpha.npy` / `ci_95_T_delta_alpha.npy`), and **`N_s`** (`N̂_s`, `se_N`, t, CI, with the note that the CI is continuous while `N_s` is an integer). New artefacts `jacobian_N_s{,_sd}.npy`, `se_theta_N_s.npy`. The joint-path `compute_T_delta_inference` keeps its own `inference_T_delta.txt` (mutually exclusive branch, untouched). **Also fixed**: `Dates.format(now(), "…SS UTC")` rendered as `AugustTC` (`U` is the full-month token) and `now()` is local time anyway — the false `UTC` suffix is dropped at all 4 report sites. **Verified**: both configurations run end to end; the granular profiled path emits all three sections and the FD block; the non-granular profiled path degrades correctly (T only, `N_s` marked not-applicable); V0 re-verified bit-identical to a contemporaneous HEAD run. |
| 2026-08-01 | `model_CP.jl`, `tools.jl`, `documentation/granular_aa_changes.md`, `CLAUDE.md` | **Fix the `hold_N_s` warning flood, and replace it with the diagnostic that was actually wanted: the Monte-Carlo dispersion of `N̂_s`.** A granular Jacobian run emitted the "the profiled variety count moved under a pinned-N̂ evaluation … θ is on a jump" warning from every worker on every evaluation — `K` replications × `2·n_perturb+1` evaluations, i.e. tens of thousands of identical lines. Two distinct defects. **(1) The warning was misattributed.** It blamed θ sitting on a jump of `N̂_s(θ)`, but the pinned value was computed on `U_DRAWS` while each Jacobian replication evaluates on INDEPENDENT draws at `N_RHO_INFERENCE`. Different draws ⇒ different `q̂` ⇒ different bisection outcome, at a FIXED θ. `compute_moments` cannot distinguish the two causes — it sees one draw set — so it no longer warns: it returns the free bisection value as a new diagnostic field `N_hat_free` beside the pinned `N_hat` (`N_hat = N_fixed === nothing ? N_hat_free : copy(N_fixed)`), and the moment blocks are untouched (`N_hat_free` is inserted AFTER `N_hat`, so the positional indices 1–6 into the returned NamedTuple are unchanged). **(2) The dispersion is a real finding and is now measured, once.** Before the replications, `compute_jacobian` evaluates `N̂_s` freely on `min(K,5)` independent draw sets built with the same `(N_RHO_INFERENCE, draw_method, base_seed)` recipe the replications use, pins the per-sector MEDIAN (previously: the single `U_DRAWS` value, at the coarser optimization `N_rho`), and prints a compact per-sector `pinned / min / max / spread` table. A spread above 10% of the pinned value warns ONCE, and says the right thing: this is simulation noise in `q̂`, not a jump, and the lever is `N_RHO_INFERENCE` (and `N_rho`, which sets `q̂` in the loss). On the reported real-data run the spread was ~45% in one sector (19 → 27), i.e. the documented "`N_rho` also sets the precision of `q̂` hence of `N̂_s`" caveat was binding and invisible. **Legacy untouched** — both edits sit inside `GRANULAR`-only branches; V0 re-verified bit-identical to HEAD on all 8 artefacts plus labels, and both configurations still run end to end (V1/V1a/V1b/V1a2/V1b2/V2a/V2b/V2c/V3 pass). |
| 2026-07-31 | `load_parameters.jl`, `model_CP.jl`, `tools.jl`, `main.jl`, `run.sh`, `test/test_granular_aa.jl`, `CLAUDE.md` | **Granular estimation switched to the fully profiled design (plan option b); `R_REP` / `n_rep` removed everywhere.** The previous design simulated `R_REP` realised economies and averaged block 4 over them, which treated simulation noise on ONE moment block by a bespoke replication device while every other block relied on the draw design (`:sobol`) — an asymmetry with no counterpart in the rest of the estimator. It was also internally inconsistent: D2 already puts the value block at the certainty-equivalent index computed on the pooled draws, so the "estimate on a realised finite economy" argument that justified replication survived in block 4 alone. **What changed.** Under the profiled estimator the variety count touches exactly two things, and both are CLOSED FORM in `q̂`: the count moment `Ḡ_s(0) = mean_l (1−q̂_ls)^{N̂_s}` and `N̂_s` itself (the monotone integer bisection on it). `q̂_ls` is free of `N_s` (Lemma 2) and is a column mean of `linkages_flat`. So **no prefix of the draws is ever taken and no replicated economy is ever simulated**: block 4 is run ONCE on the ordinary draws in both modes (it carries no `N_s` term by Prop. 1), and block 6 is the closed form — which IS the exact expectation of the realised empty-cell share (linearity over cells; the cross-cell dependence does not affect a mean), hence unbiased AND noise-free. Simulation noise is now handled uniformly across all six blocks by `DRAW_METHOD` + `N_rho`. **Deletions:** `R_REP`, `N_POOL`, `U_POOL`, `POOL_WEIGHTS`, `GRANULAR_DRAW_METHOD` (already gone), `granular_prefix_rows`, `inference_draws`, the `--n_rep` flag and the 13th positional arg of `main.jl`. `N_rho` returns to its plain meaning and value (1000, no longer pinned to `maximum(N_HI)`) — it now also sets the precision of `q̂` hence of `N̂_s`, so raise it under `GRANULAR` if the count moment looks noisy. `granular_report` drops the realised counts `K` for `EK = N̂_s·q̂` and drops `G0_closed` (`G0` IS the closed form now). Gates **V4** (closed form vs realised) and **V5** (prefix stability) are retired as vacuous; **V10** becomes a STRUCTURAL assertion — `reg_coef` must be EXACTLY invariant to `N̂_s`, not approximately — and the gate runner checks that the code path really is `N_s`-free. `hold_N_s` in `compute_jacobian` is kept (`N̂_s(θ)` is still a step function; it now affects block 6 only). **The cost of the design, stated once:** block 4 is the CONTINUUM-limit coefficient `β(E[y])` rather than `E[β̂(N_s)]`, so the auxiliary cloglog's finite-sample bias is no longer cancelled by matching a binding function. It is measured by gate V12 (2–4% of `β` in data-consistent configurations) and should be reported alongside `α̂`, or added as a local offset — `granular_validation.md` Part III. Granular estimation now costs the same as the continuum model. **Verified**: V0 still bit-identical to the pre-granular code; both configurations run end to end; the gate suite passes V1/V1a/V1b/V2/V3/V10 + `Ḡ_s(0)` monotonicity. |
| 2026-07-31 | `load_parameters.jl`, `model_CP.jl`, `tools.jl`, `run.sh`, `CLAUDE.md` | **Follow-up simplifications to the granular/AA implementation (review feedback).** (1) **`T_gravity` removed.** The Sinkhorn inversion `invert_T_from_gamma` is now the T starting value in BOTH modes; the old gravity guess `T ∝ γ·w^θ` is exactly that inversion at `α = 0` (τ ≡ 1 ⇒ market access is common within a sector and cancels), so a missing `prior_alpha` now falls back to `α = 0` instead of to a second, redundant code path. The init diagnostic keeps its comparison with the `α=0` inversion as the baseline (`T_init_alpha0_vs_inversion_a*.png`). (2) **`active_mat` renamed `supplier_cells`** with its two roles spelled out: under `:ze` it IS the estimated set, under `:aa` it only DEFINES `𝒜⁺` — the cells put into the optimisation are all of `filter == 1` inside those areas, control cells included. (3) **One draw method.** `GRANULAR_DRAW_METHOD` is gone; the granular pool uses `DRAW_METHOD` like everything else, so `:sobol` variance reduction is available. The under-dispersion caveat is now a *measured diagnostic* rather than a config flag: `granular_report` returns `G0_closed` (the closed form `mean_l (1−q̂)^{N̂}` the bisection targets) alongside the realised `G0`, and `report_granular` prints `max |G0_realised − G0_closed|` as **gate V4** with an INVESTIGATE flag above 0.05 (measured ≈0.018 with `:qmc` on the synthetic set). (4) **`N_BLOCK` removed — it IS `N_rho`.** Under `GRANULAR`, `N_rho` is bound to `maximum(N_HI)` (the per-replication variety block width, ≥ every candidate `N_s`) instead of the fixed 1000; the pool is `R_REP × N_rho` rows and a realised economy uses the prefix `ρ ≤ N̂_s`. `G_K.csv` is read in the new SECTION 2d so the bounds exist before `N_rho` is bound. (5) **`gamma_threshold` block removed** (the pruning loop, its pre-threshold diagnostic and the `gamma_threshold.npy` write), replaced by a per-sector composition table — ZE active / ZE control / ZE total / AA active / AA total — with warnings for sectors that have zero or a single supplier cell. `reconcile_sigma_data`'s pre-threshold γ-subset branch remains for old ZE-level files. (6) **`Ḡ_s(0)` now reported in `report.txt`** (`generate_dashboard_report` gains `G0_`/`granular_info`): empirical vs simulated per sector, then the profiled `N̂_s` table with `N_LO`/`N_HI`/clamp/`G0_closed`/`N^count` and the log-z coefficient against `−θ`. (7) **`--granular=true` now REQUIRES `--ca_level=aa`** — hard error in both `load_parameters.jl` and `run.sh`. Under ZE-level comparative advantage only the supplier cells are estimated, so no cell can come out empty: the count moment has no content and the estimator would be fitting `T > 0` for regions whose observed `γ_ls` is 0. The reverse (`false`, `:aa` — the AA-level continuum) stays legal and only warns. **Re-verified**: V0 still bit-identical to `HEAD`, both configurations run end to end, and the gate suite still passes V1/V1a/V1b/V2/V3/V5 + `Ḡ_s(0)` monotonicity. **Bug caught in the process**: removing the threshold block had dropped the `@everywhere const emp_gamma_ls` binding (used by `extras/` and the test files) — restored next to the load. |
| 2026-07-31 | `load_parameters.jl`, `model_CP.jl`, `model_analytical.jl`, `tools.jl`, `profiling.jl`, `optimizer.jl`, `main.jl`, `main_gmm.jl`, `run.sh`, `test/test_granular_aa.jl` (new), `documentation/`, `CLAUDE.md` | **Granular varieties + comparative advantage at the attraction-area level (`documentation/plan_granular_aa.md`), behind two independent flags.** `--granular=true\|false` (finite `N_s`, the count moment block 6, the `N_s` profiling) and `--ca_level=ze\|aa` (comparative advantage / the γ block at the ZE or ATTRACTION-AREA level). **`--granular=false --ca_level=ze` reproduces the previous estimator BIT-IDENTICALLY** — verified on a synthetic dataset against `HEAD` (simulated + analytical moment vectors, loss, `invert_T_ge` T\*, draws, `MOMENT_MASK`, `T_MASK`, moment/param labels all bitwise equal; gate **V0**). **Two index spaces** now exist and must not be conflated: **CELL space** `CELL_MASK::BitMatrix(S,R)` — the (sector, ZE) cells actually simulated (drives `n_good`, `GOOD_S/R`, `SR_TO_GOOD`, `SECTOR_GOOD_*`, `U_DRAWS` columns); and **T-COLUMN space** of width `T_COL_DIM`, the columns of the comparative-advantage matrix AND the γ-moment block (`T_ACTIVE`, `T_MASK`, `T_REF_REGION`, `SECTOR_T_COLS`, `EMP_GAMMA_T`). Under `:ze` the two coincide (legacy); under `:aa` `CELL_MASK = filter == 1` (control cells become ordinary goods with `T_{s,a} > 0` — **this is the endogenisation**) while `T_COL_DIM = n_AA` and `T_ACTIVE = AA_ACTIVE`. `T_GATHER[l]` maps ZE→T column, so `unpack_params` scatters s-major into `(S, T_COL_DIM)`, ref-normalises, then **gathers** `T[s,l] = T_par[s, T_GATHER[l]]` (identity under `:ze`); new `unpack_T_par` / `gather_T_to_ze` / `aggregate_gamma_to_T` helpers. The CLAUDE.md T↔γ alignment invariant is preserved verbatim, now over T columns. **New inputs:** `filter_N_upstream.npy` is **binary** (control cell ⟺ `filter==1 & X_rs==0`, replacing status 2 — `active_mat` is unchanged); `attraction_area_linkages.npy` `(R, R_downstream)` with three load-time assertions, the decisive one being `argmax_col == CLOSEST_DOWNSTREAM_REGION` (same partition as the empirical fixed effect); `G_K.csv` (`A129`, `K`, `G(K)`, `N_supplier_s`) giving `G_TARGET = Ḡ_s(0)` and the bounds `N_LO = ⌈N_supplier_s/R_downstream⌉`, `N_HI = N_supplier_s`. **Σ files** carry three blocks **β → γ → G** at a fixed `N_REG + n_γ + S`; `sigma_beta_gamma_filename(; smm, aa)` gains the `Sigma_aa_` PREFIX from `CA_LEVEL`, and `reconcile_sigma_data` now splits on that fixed layout, reconciles **the γ block only**, and keeps `G` only under `GRANULAR` (`G` rows are never pruned). **Estimation:** one Ricardian solve at POOL width `N_POOL = R_REP·N_BLOCK` (flat `(rep−1)·N_BLOCK + ρ` row layout, so `solve_network` needs no signature change and `U_DRAWS` IS the pool); the value block is computed over all pooled rows (the certainty-equivalent index, D2); `concentrate_N_s` profiles `N_s` by monotone INTEGER bisection on `Ḡ_s(n) = mean_l (1−q̂_ls)^n` (closed form, no re-simulation, never relaxed or rounded, clamp recorded per sector); block 4 is the firm-level cloglog on the PREFIX `ρ ≤ N̂_{s(g)}` of each replication, **averaged over `R_REP` realised economies** (matching the binding function, V12) and now also returning the `log z` coefficient (`= −θ`, a free over-identifying test); block 6 is the realised empty-cell share averaged over replications. `moments_to_vec` / `moment_blocks_tuple` replace every hard-coded `vcat(…1:5…)[MOMENT_MASK]`; `compute_block_ranges` returns 5 or 6 ranges; `inference_moment_indices` / `inference_block_layout` extend `gb_indices` to β→γ→G. `compute_jacobian` gains `hold_N_s` (pins `N̂_s` across the FD perturbations — `N̂_s(θ)` is a step function, so a central FD could straddle a jump — and `compute_moments` warns if a perturbation would have moved it) and `inference_draws` keeps the pool width under `GRANULAR`. `invert_T_ge` iterates in the T-column space (gather → closed-form ZE γ → aggregate back), so the system stays square and the contraction is inherited. Regression design flags are now effective consts: under `:aa` the control-row path is OFF (those cells are goods — appending them again would double-count) and the log-z size control is ON, which is the firm-level reduced form of Prop. 1 exactly. GMM/analytical asserts `!GRANULAR` (its extensive margin is the FKG-approximated continuum object) but does support the `:aa` γ aggregate. **Verified by running** (Julia 1.10, synthetic data): both configurations load, evaluate the loss, build `W`, compute the Jacobian and run inference end to end; new `test/test_granular_aa.jl` gate runner passes V1/V1a/V1b/V2/V3/V5 and the `Ḡ_s(0)`-monotonicity check, and reports V6/V7/V9/V10. **Not verified on the real data** (not present in this environment) — run V0 first, then the gates. **Caveat:** the granular pool defaults to `:mc` (`GRANULAR_DRAW_METHOD`) rather than `DRAW_METHOD`, because a stratified/QMC design across ρ makes prefix counts under-dispersed relative to Binomial(N̂_s, q) — biasing exactly the block-6 empty share; override with `granular_draw_method` if the QMC variance reduction on the value block is wanted instead. |
| 2026-07-23 | `tools.jl`, `CLAUDE.md` | **`compute_profiled_T_inference` now saves a T-precision-vs-γ-size scatter (γ in the renormalized Σ_r=1 domestic space).** Diagnostic for "which T are well identified": the identified T columns and the γ moment block enumerate the SAME active non-ref `(s,r)` in s-major order (T param i ↔ `gamma_rs[i]`), so per T we plot x = the DOMESTIC sourcing share `γ̃[r,s]=γ^F[r,s]/domestic_share[s]` (sums to 1 per sector — the economically comparable size, as requested) on a log axis vs y = T's precision `|t|=|T̂|/se_T`, with a 1.96 reference line. Confirms the mechanism (bigger, better-measured domestic shares ⇒ tighter T). Writes `T_precision_vs_gamma.png` + `T_precision_vs_gamma.npz` (γ̃, γ^F, T̂, se_T{,_alpha,_gamma}, t) under `inference/`. Guarded (try/catch, gated on `n_T==n_γ`); the `.npz` always writes. Verified the γ→T normalization is scale-consistent: model γ carries `*domestic_share` (⇒ `emp_gamma_ls` sums to `domestic_share`, same convention as the bootstrap `Sigma_beta_gamma`), `gamma_threshold=0` (no Julia-side rescale), and `f_γ` perturbs `emp_gamma_ls`; so `V_T=f_γ Σ_γγ f_γ'` is invariant to the with-foreign vs domestic normalization (the `1/γ` in `f_γ` cancels the `γ²` in `Σ`, and `f_γ` nulls the per-sector common-scale/foreign-split direction). Print/plot-only; no estimate changed. |
| 2026-07-23 | `tools.jl`, `CLAUDE.md` | **Bugfix: `compute_jacobian`'s α/T block diagnostics assumed the full param layout, `BoundsError` under a restricted `param_indices` (the profiled α-only Jacobian).** The "reg_coef vs α" print used `α_col = findfirst("alpha", PARAM_LABELS)` (α's position in the FULL layout, e.g. 32) to index `J_elast[m, α_col]`, but the profiled α-only Jacobian has just `N_TAU` columns ⇒ `BoundsError: 163×1 at [32,32]`. Same latent bug in the reg_coef×T noise block (`T_cols_ns = findall("T[", PARAM_LABELS)`). Both now map to the **LOCAL** perturbation order via the raw-θ layout ranges (α at `(2+S+R_downstream)…(1+S+R_downstream+N_TAU)`, T after): `alpha_local = findall(j -> α-range, indices)`, `T_cols_ns = findall(j -> indices[j] ≥ T_lo, …)`. Byte-identical for the full Jacobian (local col == PARAM_LABELS position when `param_indices==jacobian_param_indices`); the T block now correctly skips (empty) for the α-only profiled Jacobian. Print-only diagnostics; `J` unchanged. (The profiled α-Jacobian run confirmed the design: reg_coef elasticity ≈0.77 with noise 1.82 — vs 15.1 for the free Jacobian — and γ_ls elasticity ≈0.006, i.e. γ juste-identified along T*(α).) |
| 2026-07-23 | `tools.jl`, `main.jl`, `model_CP.jl`, `optimizer.jl`, `CLAUDE.md` | **Profiled inference now perturbs ONLY α (T follows via Sinkhorn) + loss-`method` and `generate_halton_grid` cleanup.** Three changes. **(1) Direct profiled Jacobian (answers "which function must account for profiling").** Verified: `build_step3_weight_matrix` (Σ_sim/W) is parameterization-independent and correctly needs NO profiling awareness; the function that must is **`compute_jacobian`**. New kwarg `profile_T::Bool=false`: when true (SMM only; asserts `!analytical`), every evaluated parameter vector is routed through `profiled_theta` first, so its T block becomes `T*(α,Ω,A)=invert_T_ge(...)`. Perturbing an α column then moves T accordingly ⇒ the returned column is the TOTAL derivative `dm/dα` along the profiled manifold — "only α perturbed, T computed accordingly" — and **no ∂m/∂T-as-free-parameter is formed**. `main.jl` (Step 2 θ̂_1, Step 4 θ̂_2) now computes a profiled α-only Jacobian `Jp = compute_jacobian(θ̂; param_indices=α-only, profile_T=true)` and passes it to `run_profiled_inference`, which **replaces** the previous chain-rule `G_α = ∂m/∂α + ∂m/∂T·∂T*/∂α` (that reused the noisy free-T Jacobian) with the direct `G_α = Jp[gb rows, :]`. `run_profiled_inference` signature drops `J_full, gb_cols` → takes `G_alpha_full, gb_indices`; the T-CI delta method (`compute_profiled_T_inference`, correlated α+γ) is unchanged and still uses `∂T*/∂α` from `_dTstar_dalpha` and `G_α` for the influence `P`. The full free-parameter Jacobian `J2`/`J1` is still computed for the diagnostics (`screen_T_identification`, `jacobian_all*.npy`); only inference switched to the profiled Jacobian. (Also removed a stray `print(DRAW_METHOD)` debug line.) **(2) Loss `method` removed.** `loss_function`/`full_SMM` dropped the `method="original"` positional (only "original" — raw `err=emp−sim` — was ever used on the live path); the `normalize`/`log` branches, `square_size`, and the `method isa Bool` shims are gone. Threaded removal through `parallel_SMM`, `parallel_SMM_safe` (its `show_err` positional shifts up one), `train_stage_one`, and `optimizer.jl` (`train_stage`/`run_optimization` `method` kwarg + all `method=method` forwards). Distinct `draw_method`/`alpha_search_method`/`REG_METHOD`/`generate_initial_alphas` "method" are untouched. **(3) Dead `generate_halton_grid` deleted.** Its only reference was `train_stage_one`'s `params_list===nothing` branch, unreachable (both callers — `generate_report`, `compute_scores_modular` — pass an explicit `params_list`); the branch now errors. **Untested here** (no local Julia); statically verified (bracket balance identical to HEAD, call-site arity, α-column ↔ `alpha_pos` alignment, `profiled_theta` late-binding safe when `profile_T=false`). |
| 2026-07-23 | `tools.jl`, `main.jl`, `main_gmm.jl`, `CLAUDE.md` | **Profiling-path inference overhaul: α CI on the α-reduced TOTAL Jacobian, correlated α+γ delta method for T (adds the γ-target data noise), + cloglog Σ_data file selection.** Three changes. **(1) cloglog Σ selection.** New `sigma_beta_gamma_filename(; smm::Bool)` (tools.jl) keys the on-disk β+γ covariance on `REG_METHOD` (`:cloglog`→`Sigma_beta_gamma_cloglog…`), `N_REG` (`==1`→`…_1…`), and the entry point (SMM→trailing `…_f.npy`, GMM→`….npy`). `build_step3_weight_matrix` (SMM) now loads `sigma_beta_gamma_filename(; smm=true)` (LPM byte-identical: `Sigma_beta_gamma[_1]_f.npy`; cloglog: `Sigma_beta_gamma_cloglog[_1]_f.npy`); `main_gmm.jl` Step 2 loads `smm=false` (`Sigma_beta_gamma_cloglog[_1].npy`). **(2) Profiled inference (SMM, gated on `profile_T`; joint path byte-identical).** Under profiling `T̂=T*(α̂,γ̂)` is the Sinkhorn image, so the joint α+T inference (which needed the noisy free-parameter `∂reg_coef/∂T` column) is replaced by: **(a) α CI on the α-reduced TOTAL Jacobian** `G_α = ∂m/∂α + ∂m/∂T·∂T*/∂α` (the total derivative along the profiled manifold — the γ_ls moments are juste-identified along `T*(α)`, isolating α on the reg_coef τ-channel); `compute_smm_inference` runs on `G_α` alone (df = n_gb − N_TAU; T shown value-only in the report). **(b) T CI by the fully-correlated delta method** `V_T = f_α Var(α̂) f_α' + f_γ Σ_γγ f_γ' + f_α Cov(α̂,γ̂) f_γ' + (·)'`, where `f_α=∂T*/∂α`, `f_γ=∂T*/∂γ` (new central log-step FD on `invert_T_ge` perturbing each `emp_gamma_ls[r,s]` target entry, `pmap` over the γ block), `Σ_γγ`=the DATA (bootstrap) γ block of `Sigma_data` (the γ-target noise the user asked to propagate), and `Cov(α̂,γ̂)=P·Σ_data[:,γ]` with the influence `P=Var_eff(α̂)·G_α'·W=dα̂/dm`. The joint `(α̂,γ̂)` covariance is PSD by construction (linear image of independent data/sim noise), so `V_T` is a proper covariance. New `tools.jl` functions: `_gamma_block_rs()` (the γ-block `(r,s)` map from `MOMENT_MASK`, aligned to `Sigma_data`'s γ rows and `emp_gamma_ls`), `_dTstar_dalpha(...)` (the `∂T*/∂α` FD, extracted from `compute_T_delta_inference` and shared — joint path unchanged), `compute_profiled_T_inference(...)` (the correlated `V_T`; writes `se_theta_T_delta{,_alpha,_gamma,_eff,_regcoef}.npy`, `jacobian_T_wrt_{alpha,gamma}.npy`, `inference_T_delta.txt`), and `run_profiled_inference(...)` (orchestrates a+b; called at Step 2 θ̂_1 and Step 4 θ̂_2 under `profile_T`). `main.jl` wraps both inference sites in `if profile_T … else <existing joint α+T inference + α-only delta counterfactual> end`. **Caveats:** the α channel uses the reported sandwich `Var(α̂)` while the γ/cross channels use `Sigma_data` (data-only) — the honest joint covariance; `f_γ` perturbs the thresholded/renormalized `emp_gamma_ls`, so `Σ_γγ` must be the covariance of the SAME γ moments (regenerate `Sigma_beta_gamma` if the active set was gamma-thresholded — the pre-existing `reconcile_sigma_data` `c_s` caveat). GMM/joint paths untouched. **Untested here** (no local Julia); statically verified (block balance, `G_α`/`f_α` row-column alignment, γ-block `(r,s)`↔`Sigma_data` γ-order, PSD-by-construction of the joint covariance). |
| 2026-07-22 | `main.jl`, `load_parameters.jl`, `model_CP.jl`, `run.sh`, `CLAUDE.md` | **Selectable extensive-margin regression method (`--reg=cloglog\|lpm`, default cloglog) + control-group toggle (`--controls=true\|false`), wired end-to-end.** New broadcast consts `REG_METHOD ∈ {:lpm,:cloglog}` and `INCLUDE_CONTROL::Bool` in `load_parameters.jl` (read from entry-point vars `reg_method`/`include_control`; defaults `:lpm`/`true` so GMM and existing callers are unchanged). `compute_moments` now dispatches block 4: `REG_METHOD==:cloglog ? fast_cloglog_regression(...; include_control=INCLUDE_CONTROL) : fast_weighted_regression(...; include_control=INCLUDE_CONTROL)`. The **empirical target is selected to match** in `load_parameters.jl` SECTION 6: cloglog → `reg_coef_cloglog_$(n_coef).npy` (or the `reg_coef_cloglog_1` stat for n_coef==1), lpm → `reg_coef_$(n_coef).npy` (or `coefs[3,"value"]`). New robust `_read_named_value(coefs_df, name)` helper (column or labeled row); `_read_prior_alpha` now reads the renamed `reg_coef_cloglog_1`. `main.jl` parses 9th arg `reg_method` (default `:cloglog`) and 10th arg `include_control` (default true). `run.sh` gains `--reg=cloglog|lpm` and `--controls=true|false` (validated, passed as args 9–10, SMM only). **Caveats:** (a) only the OPTIMIZATION loss (steps 1/3, `Weight_matrix_custom`) is auto-consistent under cloglog; INFERENCE (steps 2/4) needs a cloglog `Sigma_beta_gamma` regenerated to match, else Σ_data (LPM bootstrap) mixes with the cloglog Σ_sim. (b) GMM/analytical path (`compute_regression_quadrature`) is unchanged — LPM-convention — so keep `--reg=lpm` there (main_gmm doesn't parse `--reg` ⇒ REG_METHOD=:lpm). (c) `--controls=false` ⇒ supplier pairs only WITH the size control (coupled); its target must be the matching spec. **Untested here** (no local Julia). |
| 2026-07-22 | `model_CP.jl`, `pkg.jl`, `test/test_cloglog_verify.jl` (new), `test/test_profile_alpha_sweep.jl`, `test/files.md`, `CLAUDE.md` | **Fast cloglog extensive-margin regression (`fast_cloglog_regression`) via IRLS over the weighted-FWL kernel, + a GLM.jl correctness gate.** New `_cloglog_irls(y, X, w, fe_group; …)` fits the complementary-log-log GLM `P(y=1)=1−exp(−exp(η))` by IRLS, absorbing ONE categorical FE by weighted within-group demeaning each iteration (FWL — exact for a single FE dimension; the nonlinear analogue of `fast_weighted_regression`'s kernel). cloglog IRLS quantities: `μ=1−exp(−exp η)`, `dμ/dη=e^η(1−μ)`, working response `z=η+(y−μ)/(dμ/dη)`, weight `W=w·(dμ/dη)²/(μ(1−μ))`; η clamped for stability, FE rebuilt as weighted group means of `z−Xβ`. `fast_cloglog_regression(linkages_flat, z_flat, sample_weights; include_control, include_size_control, max_iter, tol)` is a drop-in sibling of `fast_weighted_regression` (same design: distance bins + optional log-z size control + optional control-group rows, same sector×nearest-down FE) but the correct link: **outcome is `not_supply=1−supplier`** so the distance coef is `+αθ` (the empirical `reg_coef_cloglog` convention; note sign/outcome differ from the LPM sibling). In the single-destination limit `β_distance=θα`, `β_logz=−θ`, so `include_size_control` purges the T-through-z confound and loads the distance slope on α. New standalone `test/test_cloglog_verify.jl` gates `_cloglog_irls` against **GLM.jl** `CloglogLink` (continuous regressors, bin dummies, frequency weights via row-expansion) to ~1e-5 — it takes a raw design, so no load_parameters/data needed. `GLM` added to `pkg.jl`. The α-sweep gains an additive block (C) printing MPL vs cloglog reg_coef per α (both size-controlled) under profiling, to see whether the correct link restores α-sensitivity the LPM lacked. **Untested here** (no local Julia); the GLM gate is the runtime correctness check. |
| 2026-07-22 | `model_CP.jl`, `CLAUDE.md` | **Re-add the log-z size control to `fast_weighted_regression`, coupled to the absence of the control group.** New kwarg `include_size_control::Bool = !include_control` (defaults to the coupling): the size/productivity control and the filter==2 control group are **mutually exclusive** because control firms have z≡−∞ (no productivity to regress on). So `include_control=true` (production) → control y=0 rows appended, NO log-z column (byte-identical to before); `include_control=false` (diagnostics) → supplier pairs only, log-z size control **added** as column `N_REG+1` (return is still `coefs[1:N_REG]`, dropping the size coef). Conditioning on productivity purges the T-through-z omitted-variable confound so the distance slope loads on the α/trade-cost channel (first-order α identification; see the identification discussion). An `@assert !(include_control && include_size_control)` guards the invalid combo. **Ripple:** the diagnostics that call `include_control=false` (`test/test_control_group_regression.jl`, `test/test_profile_alpha_sweep.jl`'s `reg_coef_without_control`) now evaluate the **size-controlled** regression in their "without-control" arm — the more informative object for α, but note two things change vs the "with-control" arm (control group AND size control). Still a **linear** LPM/OLS (FWL) — the correct cloglog/log-log link (proper first-order α identification) is a separate follow-up. **Untested here** (no local Julia). |
| 2026-07-21 | `test/test_profile_alpha_sweep.jl`, `test/files.md`, `CLAUDE.md` | **Add a with/without-control-group contrast to the profiling α-sweep, to test whether the extensive-margin zeros restore an interior α\* under profiling.** At each α (both PROFILED `T=invert_T_ge(α)` and FIXED-T regimes) the sweep now computes `reg_coef` **twice** — with the control-group y=0 rows (production, `include_control=true`) and without (`reg_coef_without_control` solves the network and calls `fast_weighted_regression(...; include_control=false)`). Key structural point exploited: `invert_T_ge` pins γ_ls from supplier pairs only, so `T*(α)` is **identical** in both variants — the ONLY difference is the reg_coef regressand, making it a clean isolation of the control-zeros effect. Both reg-coef losses (vs the on-disk empirical target) and both reg_coef vectors are printed per α; the verdict reports the reg_coef-loss minimiser α\* for each of {profiled, fixed-T} × {with, without} and states whether the control zeros move the profiled optimum off the α→0 boundary (hypothesis: the far-distance control zeros, which `invert_T_ge` cannot fill since control T≡0, break the τ/T\*(α) cancellation that flattens the supplier-only reg_coef). Caveat printed: the WITH-control loss is only an absolute fit if the empirical `reg_coef_*.npy` target is itself the with-control spec. Print-only, no production path touched. **Untested here** (no local Julia/data). |
| 2026-07-21 | `model_CP.jl`, `test/test_control_group_regression.jl` (new), `test/files.md`, `CLAUDE.md` | **`include_control` toggle on `fast_weighted_regression` + a with/without-control-group contrast test.** `fast_weighted_regression` gains a `include_control::Bool=true` kwarg: default (true) is byte-identical to production (appends the `N_CONTROL` filter==2 y=0 rows); false builds the design from supplier pairs only. The log-z size control is dropped in **both** regimes (the toggle only adds/removes the control-group rows, never the productivity column). `N_valid` sizes off `n_ctrl_eff = include_control ? N_CONTROL : 0` and the control loop is guarded — no undef rows when off. All existing callers (`compute_moments`) pass positionally ⇒ default true ⇒ unchanged. New standalone print-only `test/test_control_group_regression.jl` loads a saved θ̂ (step3/theta_hat_2 → step1/best_params, run folder as optional 4th arg), solves the network once, and evaluates the regression WITH vs WITHOUT the control group, printing both coefficient vectors, their Δ, the empirical `reg_coef` target, and the per-bin supplier/control pair composition. (Also reverted the earlier stray 3-bin branch in `distance_bin` — that was a data-side mistake, `reg_coef_4.npy` should carry 4 coefficients.) **Untested here** (no local Julia/data). |
| 2026-07-21 | `load_parameters.jl`, `model_CP.jl`, `model_analytical.jl`, `CLAUDE.md` | **Exogenous extensive-margin zeros via `filter_N_upstream` 3-status + drop the size (log-z) control from the reg_coef regression.** Empirically the extensive-margin distance decay is estimated including control-group regions that have NO suppliers (y=0); the model regression previously only saw active supplier pairs. First-pass (exogenous, not GE-endogenized) fix keyed on the integer `filter_N_upstream[s,r] ∈ {0,1,2}` status matrix (already loaded, values now USED not just for `size()`). **(1) Active set (`load_parameters.jl` SECTION 4):** `T_MASK`/`T_MASK_MOMENT` now come from `active_mat = (filter_N_upstream .== 1) .& (X_rs > 0)` (was `X_rs > 0` alone) — status 0 dropped as before, status 2 (control-only) excluded from the T active set and every moment block. All downstream sets (`n_good`, `GOOD_S/R`, `SECTOR_GOOD_*`, `SR_TO_GOOD`, `T_REF_REGION`, gravity/γ-inversion inits, `MOMENT_MASK`, `jacobian_param_indices`, labels) inherit the restriction automatically (they all derive from `T_MASK`). The gamma-threshold block (which zeroes `X_rs`) still composes correctly since `active_mat` requires `X_rs>0`. New broadcast consts `CONTROL_S`/`CONTROL_R`/`N_CONTROL` enumerate the `filter==2` pairs (s-major, mirroring `GOOD_S/R`). **(2) SMM regression (`model_CP.jl fast_weighted_regression`):** `n_regressors = N_REG` (was `N_REG+1`) — the log-z size/productivity control is **removed** as a regressor (control pairs have z≡−∞); `z_flat` is still built by `solve_network` for the Ricardian selection, just not regressed on. After the `n_good` supplier loop, appends `N_CONTROL × N_rho_eff` rows of `y=0` at each control pair's distance-to-nearest-downstream bin, sharing the `(sector × nearest-down)` FE, flat weight `1/N_rho_eff` (⇒ total weight 1 per control pair, matching a supplier pair whose `sample_weights` column sums to 1). **(3) GMM/analytical (`model_analytical.jl compute_regression_quadrature`):** mirrors (2) — `n_reg = N_REG`, drops the `log(z_k)` column, appends one `y=0` row (weight 1) per control pair; type-generic (`zero(FT)`/`FT(...)`) so the ForwardDiff AD path stays differentiable. `coefs[1:N_REG]` return unchanged (design is now exactly N_REG columns). **Caveats:** (a) the empirical `reg_coef_*.npy` **target must be recomputed** with the same spec (distance bins, no size control, control regions as y=0) or the model fits a different object — a data-pipeline change outside this repo. (b) `N_REG`/`BLOCK_RANGES[4]`/`Sigma_beta_gamma`/Jacobian sizes are all **unchanged** (only the regressor set and appended rows change). (c) the standalone diagnostic `test/test_extensive_margin.jl reg_coef_quad` still assumes the old `N_REG+1` design and is now out of sync (print-only, not on the production path). **Untested here** (no local Julia). |
| 2026-07-18 | `optimizers/pso_integration.jl`, `optimizer.jl`, `profiling.jl`, `test/test_profile_alpha_sweep.jl`, `CLAUDE.md` | **PSO per-report T-convergence count under profiling + two fixes to `test_profile_alpha_sweep.jl`.** **(1) Convergence report:** the PSO periodic log (the existing every-5-iter `verbose` block in `parallel_pso_smm`) now prints `T non-converged: n_bad/n_tot particles (p%)` when profiling — how many particles in the current swarm carry a fallback (non-converged) `invert_T_ge` T. Plumbing: `profiling.jl` splits `profiled_theta` into `profiled_theta_full` (returns `(theta, converged, resid, iters)`) + a cheap `profiled_theta_converged(x_levels)::Bool` probe (skips the T scatter; try/catch ⇒ error counts as non-converged); `optimizer.jl` factors the objective's stage→full reconstruction into `build_x_full` (shared by the objective and the probe), builds `t_conv_probe = profile_T ? (x_stage->profiled_theta_converged(build_x_full(x_stage))) : nothing`, and threads it through `optimize_stage` (new kwarg) to `parallel_pso_smm` (new kwarg `t_conv_probe`); PSO runs `pmap(p->t_conv_probe(_fwd(p)), particles)` at report time (mirrors the `obj=objective_func∘_fwd` composition, only on report iters). CMA-ES/TikTak branches are untouched (probe not forwarded). `profile_T=false` ⇒ probe `nothing` ⇒ byte-identical PSO log. **(2) Test fixes:** `eval_moments` used `full_SMM(θ, true; …)` (simulation=true) which returns the **X_ls trade matrix**, not the moments tuple → `vec(::Float64)` MethodError; now `_, sim = full_SMM(θ; …)` (simulation=false → `(loss, moments)`). And `output_folder` no longer hardcodes `./reporting_<industry>`; it defaults to the **non-profiled PSO** run `./reporting_<industry>_pso` (free-T fit, α≈0.30 — the θ̂ whose T̂ regime (B) holds fixed), with an optional 4th positional arg to point at any run (e.g. `reporting_<industry>_profiled_pso`). **Untested here** (no local Julia). |
| 2026-07-18 | `tools.jl`, `main.jl`, `main_gmm.jl`, `test/test_profile_alpha_sweep.jl` (new), `test/files.md`, `CLAUDE.md` | **(1) Inference report shows the non-inferred head params (Ω^L, Ω^s, A) after T, value-only (no CI); (2) diagnostic for why Sinkhorn-profiling pushes α→0.** **(1)** `compute_smm_inference` (`tools.jl`) gains two kwargs `display_labels`/`display_values`; after the active-param (α+T) table it prints a new `--- Other parameters (not inferred; value only, no CI) ---` block listing each head param's θ with "-" in the se/t/CI columns (inference does not target them, so no interval is implied). Wired at all 4 SMM+GMM inference call sites (`main.jl` step2/step4, `main_gmm.jl` step2/step4): `head_cols = findall(i -> i < alpha_T_start, jacobian_param_indices)` selects the identified Ω/A columns, values pulled from `theta_hat_*`. Additive — active-param table, files, and all estimates unchanged. **(2)** New standalone print-only `test/test_profile_alpha_sweep.jl`: at a fixed head from θ̂ it sweeps α and contrasts the **PROFILED** regime (`T=invert_T_ge(α)`, γ_ls pinned ∀α) vs a **FIXED-T** regime (`T=T̂`), printing reg_coef/γ_ls/β+γ losses + the reg_coef vector per α. Demonstrates the mechanism: with T fixed, α has an interior reg_coef optimum (~0.3, the τ=d^α trade-cost channel identifies it); once T is profiled, the exact-γ_ls constraint forces `invert_T_ge` to raise remote-origin T as α grows, which **flattens reg_coef and cancels α's τ-channel leverage**, so the reg_coef optimum collapses to the α→0 boundary (τ≈1 ⇒ clean matrix-scaling inversion). Root cause: **profiling T out profiles out most of α's identification** — the joint (soft-weighted) search keeps some γ_ls misfit to let α drive reg_coef (α≈0.30), the profiled (hard-γ_ls) search cannot. **Untested here** (no local Julia). |
| 2026-07-16 | `load_parameters.jl`, `tools.jl`, `main.jl`, `main_gmm.jl`, `run.sh`, `CLAUDE.md` | **Decouple inference draw count from optimization (`N_RHO_INFERENCE`); bump FD Jacobian `step_rel` 1e-4→1e-2; + two persistent inference caveats (AD-α FKG bias, per-sector `H[T,T]`).** **(1) `N_RHO_INFERENCE`** — new `@everywhere const` in `load_parameters.jl` (entry point may define `n_rho_inference`; default `= N_rho`, so byte-identical unless set). `tools.jl` now resamples the **Σ_sim** estimate (`build_step3_weight_matrix`) and the **fixed-draw Jacobian** replications (`compute_jacobian`) with `generate_draws(N_RHO_INFERENCE, …)` instead of the optimization `N_rho`; `full_SMM` already sizes off `size(u_draws,1)` (the 2026-06-18 `N_rho_eff` fix), so passing a different row count is safe. The optimizer's loss (`U_DRAWS`) still uses `N_rho`. Motivation: inference is simulation-noisy (the report's `reg_coef` Jacobian noise ≈ 13.5) and benefits from far more draws than the search, without slowing optimization. Wired end-to-end: `main.jl` 8th positional arg `n_rho_inference` (default 10000); `run.sh` `--n_rho_inf=N` (passed as arg 8) + help; `main_gmm.jl` inert (analytical Jacobian uses no draws, Σ_sim=0). **(2) `step_rel` 1e-4 → 1e-2** at all 9 sites (the `compute_jacobian` default in `tools.jl`, all 4 `main.jl` + 2 `main_gmm.jl` call sites, 3 `test/run_internal_validity.jl` sites) — the log-space central FD step is now 1e-2. **(3) Two persistent caveats** added to a new `## Critical caveats` section (so they aren't lost in the changelog): (a) the **analytical/AD Jacobian is FKG-biased on α / the whole `reg_coef` block** — use the *simulation-based* Jacobian for α SEs/identification, never AD/`analytical=true`; the AD-vs-FD `O(δ²)` agreement does NOT clear this (shared FKG regressand). (b) **`H=J'WJ` on T is block-diagonal by sector** — read the per-sector `H[T_s,T_s]`/`M^s` diagonal spectra, not the global `H[T,T]` λ_min/cond (whose cross-sector coupling is a weighting/units artifact). **Untested here** (no local Julia). |
| 2026-07-16 | `optimizer.jl`, `optimizers/tiktak_integration.jl`, `optimizers/files.md`, `CLAUDE.md` | **TikTak now runs PSO's staged block-coordinate refinement (successive productivity → α → T → technical steps), not a single joint run; + dimension-aware pre-test.** Follow-up to the TikTak backend. **(1) Refinement (`optimizer.jl run_optimization`):** the refinement-skip switch reverts to `run_refinement = gamma_beta_only || OPTIMIZER_BACKEND != :cmaes` — TikTak (like PSO) now runs the joint-path block-coordinate loops, so after the joint Stage-1 fit it *successively* refines each parameter block (productivity, α, technical under `profile_T`; +T when not profiled), each sub-stage re-running TikTak on one block warm-started at the incumbent and monotone-floored. **Only CMA-ES** still collapses the non-`gamma_beta_only` path into the single Stage-1 run (it learns cross-block covariance directly). The `global_backend` local and the CMA-ES/TikTak-skip comments are removed/updated; `substages_per_loop` unchanged. **(2) Dimension-aware pre-test (`tiktak_integration.jl`):** `n_sobol = clamp(points_per_dim·d, K, sobol_multiplier·K)` (new kwarg `points_per_dim=500`) replaces the flat `sobol_multiplier·K`. A high-dim joint stage still hits the `sobol_multiplier·K` cap (unchanged), but a low-dim block-coordinate sub-stage (e.g. the 1-D α stage, where a 10k-point pre-test saturates and the local searches can't improve) now uses `points_per_dim·d` points — so the newly-enabled successive-block refinement stays cheap instead of re-running a saturated pre-test per sub-stage. `sobol_multiplier` is now the *cap* multiplier; docstrings + `files.md` updated. **Untested here** (no local Julia); the report from the prior run motivated both changes (1-D α sub-stage local searches were flat vs pre-test; the joint step-1 skipped refinement entirely). |
| 2026-07-16 | `optimizers/tiktak_integration.jl` (new), `optimizer.jl`, `main.jl`, `main_gmm.jl`, `load_parameters.jl`, `run.sh`, `pkg.jl`, `optimizers/files.md`, `CLAUDE.md` | **New `--optimizer=tiktak` backend: TikTak global multistart (Sobol pre-test + Nelder-Mead), for the Sinkhorn-reduced (`profile_T`) parameter space.** ADDITIVE — PSO/CMA-ES paths byte-identical; the backend only runs when `optimizer_backend=:tiktak` is selected. **New `optimizers/tiktak_integration.jl`** exposes `parallel_tiktak_smm(objective, lb, ub; x0, ...)` honoring the same contract as PSO/CMA-ES (`(best_x, best_f, history)`), so it drops into `optimize_stage` with no caller change. Faithful adaptation of Guvenen's TikTak ([serdarozkan/TikTak](https://github.com/serdarozkan/TikTak)): **(1) pre-test** — evaluate a scrambled-Sobol net (reusing `sobol_scrambled_net`) of `sobol_multiplier·K` points (default 100·K), `pmap`-parallel, keep the best `K=n_particles` *legitimate* (finite) points sorted ascending by loss; **(2) sequential local search** — walk the kept points best→worst, starting each local solve not from `sᵢ` but from the convex blend `z_startᵢ=(1−θᵢ)·sᵢ+θᵢ·z*` toward the incumbent best `z*`, with `θᵢ=clamp((i/K)^{1/2}, 0.1, 0.995)` (helper `tiktak_blend_weight`) growing in the restart index so late/worse starts *refine* `z*`. Runs on the **normalized unit cube** `[0,1]ⁿ` mapped back to `[lb,ub]` inside the evaluator (same trick as CMA-ES — Nelder-Mead's single-scale simplex needs comparable coordinates; uses the **current `train_stage` bounds** unchanged), with `enforce_alpha_constraint` α-repair in real space and a warm-start floor (x0 injected as a pretest candidate + compared against `f(x0)` at the end ⇒ never worse than the warm start, matching the staged-pipeline monotonicity guarantee). **Local solver = Nelder-Mead via `Optim.jl`** (already a dependency — no new package) for d≥2, **Brent** over the unit interval for the 1-D single-α `gamma_beta_only` sub-stage (Optim's simplex needs ≥2 dims); `local_search` is factored out so a DFNLS/BOBYQA engine (e.g. NLopt `LN_BOBYQA`) can replace it without touching the driver. **Parallelism**: pre-test fully `pmap`-parallel; local searches run in **batches of `nworkers()`** (each search serial on one worker — since `parallel_SMM_safe` is itself serial — blending toward the incumbent as it stood at batch start, the standard parallel-TikTak trade-off); `nworkers()==1` ⇒ fully sequential exact TikTak. Budget mapping: `n_particles→K` restarts, `max_iter→` Nelder-Mead iterations/search. **`optimizer.jl`**: `optimize_stage` gains a `:tiktak` dispatch branch (passes `seed`/`verbose` like CMA-ES); the refinement-skip logic now treats TikTak like CMA-ES as a *global* backend (`global_backend = OPTIMIZER_BACKEND in (:cmaes,:tiktak)`; `run_refinement = gamma_beta_only || !global_backend`) — one joint search per SMM step, skipping the PSO block-coordinate loops, except the `gamma_beta_only` α-then-T alternation which every backend runs; docstrings updated. **`main.jl`**: `@everywhere include("optimizers/tiktak_integration.jl")`; arg assert widened to `(:pso,:cmaes,:tiktak)`. **`load_parameters.jl`**: `OPTIMIZER_BACKEND` assert widened to `:tiktak`. **`main_gmm.jl`**: include added for parity (inert; GMM defaults `:pso` and doesn't parse `--optimizer`). **`run.sh`**: `--optimizer=pso|cmaes|tiktak` validation + help + example (`--optimizer=tiktak --profile_T=true`). **`pkg.jl`**: comment noting Optim powers the TikTak local solver (already installed). **Untested here** (no local Julia); statically verified: block/paren balance, contract parity with `parallel_cmaes_smm`, unit-cube map + α-repair + warm-start floor mirror CMA-ES, `Optim`/`sobol_scrambled_net`/`enforce_alpha_constraint` resolve at call time via the `@everywhere include`s. **To run:** `./run.sh aero --n_coef=4 --n_tau=1 --optimizer=tiktak --profile_T=true` (SMM only), or set `optimizer_backend=:tiktak` in `main.jl`; tune restarts via `n_particles`, NM budget via `max_iter`, pretest density via the `sobol_multiplier` kwarg. |
| 2026-07-16 | `profiling.jl`, `optimizer.jl`, `main.jl`, `test/test_ge_inversion.jl`, `CLAUDE.md` | **T-profiling — Phase 0 gate result (GO), two solver tweaks, + Phase 2/3 wiring behind `profile_T` (default `false` ⇒ byte-identical).** **Phase-0 gate (aero, N_REG=4, N_TAU=1) passed:** round-trip `1e-16`, uniqueness `1e-11` (10/10 starts), and — despite `ν=0.2, λ=0.5` far from Cobb-Douglas — the endogenous-expenditure map is a *strong* contraction: `κ_S≈0.005`, `ρ_full≈0.012`, `‖J_GE‖≈0.015` (`κ_S+‖J_GE‖≈0.02≪1`). The §4 worry (`‖J_GE‖∝|1−ν|,|1−λ|`) was an upper-bound heuristic that does not bind in this geometry; no robustification needed. Cost: `invert_T_ge≈0.35×` one analytical eval, `≈1.2%` of one `full_SMM` loss. **Tweaks:** (1) `invert_T_ge` default `damping 0.5→0.9` — the undamped map contracts at ~0.01, so 0.5 was needlessly slow (real-data inversion 31→~5 iters). (2) `test/test_ge_inversion.jl` round-trip now starts from a ×LogUniform[0.5,2]-perturbed T (genuine recovery, not the identity), which also populates the residual history (the earlier `NaN` observed-contraction was an artifact of converging in 1 step). **Phase 2 (`optimizer.jl`):** `train_stage`/`run_optimization` gain `profile_T::Bool=false`. When true, the T (φ) block is dropped from the fresh-start search box and warm start (head-only, `[Ω^L\|Ω^s\|A\|α]`; α position/constraint unchanged since T was the trailing block), and every objective + final reconstruction routes through the new top-level `profiled_theta(x_levels)` (in `profiling.jl`; top-level so it serializes across the PSO `pmap`) which overwrites the T block with `invert_T_ge(normalized head)`. The standalone `["T"]` sub-stages are skipped (gamma_beta_only loop `("alpha","T")→("alpha",)` and its initial decoupled T fit; joint-path sub-stage 2b), and `substages_per_loop` drops `2→1` / `4→3` so the epoch-to-epoch convergence check still points at the previous loop's final stage. `_fwd`/`log_mask` are identity for these dims (verified: `optimize_stage` passes no `log_mask`), so dropping the φ_T tail is coordinate-safe. **Phase 3 (inference) needs NO mechanical change:** Step 4 already computes the full Jacobian over all params and restricts columns to α+T (`gb_cols = findall(i≥alpha_T_start)`), so at the profiled θ̂_2 (which carries `T*(α̂,Ω̂,Â)`) the standard GMM `G'WG` returns T's *joint* CIs — no delta method (a clarifying comment was added). **`main.jl`:** new `profile_T=false` flag; `@everywhere include("profiling.jl")` (inert unless the flag is on); `output_folder` gains a `_profiled` suffix when profiling so joint-search artifacts are never overwritten; `profile_T` threaded into both `run_optimization` calls. **`main_gmm.jl` intentionally untouched** (Design A is SMM-only — the loss's reg_coef must stay simulation-based; GMM's analytical reg_coef is FKG-biased). **Untested here** (no local Julia); statically verified: profile_T=false leaves every branch on the historical path; layout indices match `get_param_start_index`; s-major/ref-gauge (`T*[s,ref]=1`) consistent with `unpack_params`. **To run:** `run.sh --profile_T=true` (SMM only; 7th positional arg to `main.jl`, `industry n_coef n_tau K_sim draws optimizer profile_T`; ignored under `--mode=gmm`), e.g. `./run.sh aero --n_coef=4 --n_tau=1 --profile_T=true`; or set the `profile_T` flag in `main.jl` directly. Compare `α̂/Ω̂/Â` and especially the T CIs in `reporting_aero_profiled/step3/inference/` against the joint `reporting_aero/`. |
| 2026-07-16 | `profiling.jl` (new), `test/test_ge_inversion.jl` (new), `CLAUDE.md` | **T-profiling — Phase 0 (feasibility gate) + Phase 1 (internal solver). ADDITIVE ONLY; zero production path touched.** Groundwork for profiling the comparative-advantage block **T** out of the outer PSO search via a GE-Sinkhorn inversion of `γ_ls` (the plan is in the branch header; theory in `documentation/initialisation.md`). **Design A** (exact `γ_ls` inversion — the plan's recommendation): to each particle `(α, Ω^L, Ω^s, A)` the profiled estimator will assign the `T*(α,Ω,A)` reproducing the observed sourcing shares, collapsing the outer search from ~hundreds of dims to ~tens; the `γ_ls` block becomes juste-identified and `reg_coef` is fit by `(α,Ω,A)` alone. **The inference (Step 4) keeps T in the full Jacobian** (Phase 3, not yet wired) so T's CIs are preserved — profiling changes only the *search*, not the *inference*. (1) **New `profiling.jl`** (root; will be included behind a `profile_T` flag in Phase 2): `gamma_ls_analytical(Ω^L,Ω^s,A,T_mat,tau)` replicates ONLY blocks 2&5 of `compute_moments_analytical` (the exact closed-form `γ_ls` via `compute_prices_analytical`) — it **never** calls `compute_regression_quadrature` (FKG-biased, plan §7.1); `invert_T_ge(alpha,Ω^L,Ω^s,A; target=emp_gamma_ls, T_init=copy(T_rs_init), max_iter, tol, damping=0.5)` runs the damped multiplicative Sinkhorn update `T⁺[s,r]=T[s,r]·(target/γ_model)^δ` with **endogenous** expenditure `ω=exp_{s,dr}(T)` (via the price solve), ref-normalized per sector each pass (gauge `T[s,ref]=1`, matching `unpack_params`), returning `(T::(S,R), iters, converged, resid, resid_hist)` — deterministic (no PSO noise), `converged=false` signals fall-back on the warm start; `assemble_theta(Ω^L,Ω^s,A,alpha,T_mat)` builds the full level θ with the reduced T block `vec(permutedims(T_mat))[T_MASK]` (s-major, matches `optimizer.jl:230`). (2) **New `test/test_ge_inversion.jl`** (standalone, print-only, includes model files + `load_parameters.jl` like `test_extensive_margin.jl`; loads a saved θ̂ or a documented default): the **Phase-0 go/no-go gate**. Measures (1) **round-trip** (recover `T_true` from its own `γ_ls` to ~1e-8), (2) **convergence** — by finite differences on the one-step map Ψ: `ρ_frozen≈κ_S` (Birkhoff contraction, expenditure held fixed at `T*`), `ρ_full` (endogenous map spectral radius — GATE `ρ_full<1`), and `‖J_GE‖=‖DΨ_full−DΨ_frozen‖₂` (the GE channel, §4), plus the sufficient bound `κ_S+‖J_GE‖<1` and the observed residual-ratio contraction, (3) **uniqueness** (10 ×LogUniform[0.5,2] warm starts → same `T*`?), (4) **cost** (`invert_T_ge` vs one `full_SMM` loss vs one analytical eval). **Motivating finding**: the calibration is `ν=0.2, λ=0.5` (`load_parameters.jl:61-62`) — **far from Cobb-Douglas** (ν=λ=1), so `‖J_GE‖∝|1−ν|,|1−λ|=0.8,0.5` is NOT a small perturbation of pure Sinkhorn; convergence/uniqueness of the exact inversion is therefore an **empirical** question the gate must settle before Phase 2/3 wiring. **Untested here** (no local Julia/data); statically verified (no name collisions with production; s-major/ref-gauge alignment with `unpack_params`/`T_MASK`; `compute_prices_analytical`/`compute_Phi`/`build_tau` signatures). **Pending**: Design-A confirmation with the user, the Phase-0 gate result, then Phase 2 (flag + optimizer wiring) and Phase 3 (full-Jacobian inference at the profiled point). |
| 2026-07-15 | `tools.jl`, `main.jl`, `main_gmm.jl`, `test/run_internal_validity.jl`, `documentation/inference.md`, `test/files.md`, `CLAUDE.md` | **Jacobian: ONE log step for every column (additive step + inert vars removed) + expanded `test_extensive_margin.jl` doc.** (1) **`compute_jacobian` (`tools.jl`)**: the FD Jacobian previously routed only the T columns through the scale-invariant log-space central step (`θ·exp(±δ)`, chain-ruled back to raw units by `1/θ`) and every other column (`Ω^L`, `Ω^s`, `A`, `β`/`α`) through an **additive** step `h=max(|θ|·step_rel, step_abs)`. Now **all** columns take the **same** log step `δ = step_rel` and the additive branch is deleted. `δ` is a scalar (was a per-column vector); `use_log`/`is_T`/the `h` array are gone; the log perturbation, chain-rule division, forward-backward symmetry increments, and Richardson step-doubling all run unconditionally over every column (`log_cols`→`all_cols = 1:n_perturb`; the `use_log[kk] || continue` guard on Richardson dropped; the non-finite-error regime label is now always `[log]`; print headers say "all columns"). `T_first` is kept only for the Richardson per-sector diagnostic's `param_sector`. The `θ>0` assertion now guards **every** active column (was T-only) — log step needs `θ·exp(±δ)>0` and finite `1/θ`; T_MASK excludes zero T and the head params are positive at any sane estimate, so it should not fire, but fails loudly if a non-positive column is ever perturbed. The now-inert **kwargs are removed** from the signature and every call site: `step_abs` (floored only the deleted additive step), `t_log_step_rel` (the separate T step, no longer needed), and `t_log_step` (no longer toggles a regime). Call-site edits: `main.jl` (4 sites), `main_gmm.jl` (2), `test/run_internal_validity.jl` (3) drop `step_abs=1e-9`; the AD-validate recursion no longer forwards `t_log_step`/`t_log_step_rel`/`step_abs`. **Effect**: T-column SEs shift slightly (log step is now `step_rel=1e-4` instead of the old `1e-2`), non-T SEs shift slightly (log vs additive truncation at the same relative magnitude — the intended correction, scale-invariant and floor-immune); the stored column stays raw `∂m/∂θ`, so `G'WG` inference is unchanged in units. Docstring rewritten to a single log-step regime; `documentation/inference.md` updated to match. The AD path (`analytical_ad`) and the `analytical=true` FD path are unaffected structurally (the FD path shares this column logic). **Untested here** (no local Julia). (2) **`test/files.md`**: added an in-depth section documenting `test_extensive_margin.jl` — the FKG-vs-exact motivation, the `Q`/`Φ(S)`/`D*` math, the self-check, and all five printed screens (`\|D*\|` histogram + G2 decision, MC/FKG/exact-IE/pairwise error table at the high-productivity tail, the dominance-pruned memoized union DAG and its `nsub` feasibility count, β-perturbation `\|D*\|` stability, and the `reg_coef` FKG-vs-exact coefficient-bias gate) plus its purely-diagnostic status. |
| 2026-07-10 | `README.md`, `documentation/model.md`, `documentation/inference.md` (new), `CLAUDE.md` | **Doc corrections + new inference.md** (docs only; no code touched). (1) **Step 1 flow corrected** in `README.md`: removed the wrong "Stage 0 α search" — α is **not** searched; it is read from the extensive-margin distance regression (`prior_alpha` in `stats.csv`, bound as `P_alpha`), T is then initialized by the closed-form γ-inversion given that α (`invert_T_from_gamma`), and only then does the global joint search run (`main.jl` calls `run_optimization(skip_initial_alpha_search=true, warm_start=[…,P_alpha,T_init_nz])`). (2) **α/β notation fixed** in the `README.md` and `model.md` parameter tables: the only trade-cost parameter is **α** (`N_TAU` of them); the `reg_coef` distance coefficients (block 4, written β in the regression) are **moments, not parameters** — `N_REG` and `N_TAU` are independent. (3) **`model.md` new section 5b — per-sector Sobol draws**: documents why the default `--draws=sobol` is non-standard — a single global Sobol net correlates coordinates across regions and biases the Ricardian `min`, so the code uses one low-dimensional Sobol net *per sector* with a base-2 digital-shift scramble that drives cross-region correlation to ≈1/√N ("mimics independence"), with flat weights keeping the min-coupled moments unbiased. (4) **New `documentation/inference.md`**: derives SMM inference — data noise `Σ_data` (bootstrap) vs simulation noise `Σ_sim` (K re-simulations), `Ω=Σ_data+Σ_sim`, `W=Ω^{-1}`; the Jacobian `G=∂m/∂θ` (fixed-draw central FD, log-step for the positive multiplicative T columns, exact AD in GMM, `step_rel=1e-4`); efficient `V_eff=(G'WG)^{-1}` vs sandwich `V_sw=(G'WG)^{-1}G'WΩWG(G'WG)^{-1}` (equal iff `W=Ω^{-1}`, ratio is a diagnostic); Hansen J; and the full list of `inference/` reports. Cross-linked from `README.md` and `model.md`. |
| 2026-07-10 | repo layout, `README.md`, `documentation/`, `optimizers/`, `test/`, `extras/`, `CLAUDE.md` | **Repository restructure + documentation refresh for handoff.** Moved files into role-based folders and rewrote the reader-facing docs; **no source logic changed** — only `include()` paths and file locations. **Root now holds only the optimization core**: `main.jl`, `main_gmm.jl`, `model_CP.jl`, `model_analytical.jl`, `load_parameters.jl`, `optimizer.jl`, `tools.jl`, `pkg.jl`, `run.sh`, `analysis.ipynb`. **New folders**: `optimizers/` (`pso_integration.jl`, `cmaes_integration.jl`); `test/` (`run_test.sh`, `run_internal_validity.jl`, `run_untargeted_validation.jl`, `test_extensive_margin.jl`, `test_t_reorder.jl`); `extras/` (`untargeted_moments.jl`, `compute_amplification_weights.jl`, `run_amplification_analysis.jl`); `documentation/` (`model.md`, `optimizer.md`). Each of `optimizers/`, `test/`, `extras/` gets a `files.md` role guide. **Deleted**: `main_pso.jl` and lowercase `claude.md` (both superseded), `nohup.out` (stale log), and the old `doc/`, `notes/` folders. **`include()` rewrites** (the only code edits): `main.jl`→`optimizers/pso_integration.jl`, `optimizers/cmaes_integration.jl`, `test/run_untargeted_validation.jl`; `main_gmm.jl`→`optimizers/…`; every `test/*.jl` uses `../` for root files and `../optimizers/`, `../extras/` for the rest; `extras/*.jl` uses `../model_CP.jl`/`../tools.jl`; `test/run_test.sh` and `run.sh` script paths updated. Julia resolves relative `include` against the *including file's* directory, so nested chains (e.g. `main.jl`→`test/run_untargeted_validation.jl`→`../extras/untargeted_moments.jl`) resolve correctly. **`README.md` fully rewritten** (goal, structure, parameters, `run.sh` usage + example); **`documentation/model.md`** (model + math, economist-level) and **`documentation/optimizer.md`** (PSO and CMA-ES mechanisms/parameters/usage) authored fresh. **Untested here** (no local Julia); verified statically that all 39 `include()` targets resolve on disk. |
| 2026-07-08 | `optimizer.jl`, `tools.jl`, `CLAUDE.md` | **Tighten the α (β)/T search box to ±20% and add a per-epoch "best T vs very-initial T" scatter (α in the name).** (1) **Tighter box** (`optimizer.jl train_stage`): `BOUND_LO, BOUND_HI = 0.8, 1.2` (was `0.5, 2.0`), so α and T are now boxed to `[×0.8, ×1.2]` × their **initial value** in every stage — T to `T_rs_init` (via `phi_anchor .+ [log0.8, log1.2]`), β to the α prior `TAU_PRIOR`. The continue-stage per-stage-radius clamps and the fresh-start box inherit the constants unchanged; only the endpoints move. **Stage-0 β search** (`run_optimization`, N_TAU==1 & prior) re-anchored to `prior×[0.8,1.2]` so the selected `init_beta` still lands inside the Stage-1 box. **Warm-start-safe**: the step-1 warm start sits at the T/β anchor centre and every incumbent was optimized within the same (now tighter) box, so `lb<ub` and the clamp stay feasible Step1→Step3. (2) **Per-epoch T plot** (new `tools.jl plot_T_vs_initial(best_params, out_folder; label)`): a guarded (`try`/`catch`, never blocks estimation) log-log scatter of the current best T (`unpack_params(best_params)` T block, per-sector ref-normalized) on **y** vs the very-initial `T_rs_init` (the γ-inversion warm start at the α prior, also ref-normalized) on **x**, over all active `(s,r)`. The **current best α** (β block, `TAU_PRIOR` for the initial) is embedded in the title, both axis labels, and the filename `T_best_vs_initial_alpha<α>.png` (+ companion `.npz` with the raw pairs and both α's). Called **inside `generate_report`** as a second plot alongside `dashboard.png`, so it is written into every stage/epoch report folder (loop base, `epoch_$loop/$stage`) for both SMM and GMM automatically — the report already has the selected `best_params` and `folder`. Additive/print-only for estimation — no change to `solve_network`/moments/inference/weights; only the bounds endpoints and the new diagnostic move. **Untested here** (no local Julia); `Plots`/`NPZ` calls mirror the existing `load_parameters.jl` T-init diagnostic and `generate_report`. |
| 2026-07-08 | `load_parameters.jl`, `optimizer.jl`, `CLAUDE.md` | **Init-anchored PSO search box for α (β) and T: [×0.5, ×2] of the initial value in every stage.** Before this, the fresh-start T box was the absolute `φ∈[log0.5, log2]` (⇒ T∈[0.5,2]) and β was `init_beta×[0.5,1.5]`, with the continue-stage clamps `φ∈[log0.5,log2]` / β∈[0.5,1.5] — all **anchored to T≡1**, which was valid only for the old all-ones `T_rs_init`. The γ-inversion init now spans a range (e.g. 0.5–3×), so those absolute boxes **excluded feasible init values** (a region with T_init=3 fell outside [0.5,2]) and were asymmetric for α. **Fix** (`optimizer.jl train_stage`): both α and T are boxed to `[BOUND_LO,BOUND_HI]=[0.5,2]` × their **initial value** — T to `T_rs_init` (via the free-φ anchor `phi_anchor = t_levels_to_free_phi(vec(permutedims(T_rs_init))[T_MASK])`, box `φ_anchor .+ [log0.5, log2]`), β to the α prior `TAU_PRIOR` (new `load_parameters.jl` const = `[prior_alpha]` when `N_TAU==1` & prior present, else `nothing`). **Fresh start**: T block = `T_phi_lo/T_phi_hi`, β = `beta_anchor×[0.5,2]` (`beta_anchor = TAU_PRIOR ?? init_beta`). **Continue stages**: the per-stage radius `alpha` still forms the inner box (`val±|log α|` for T, `val×(1±α)` for β) but is **clamped to the same init-anchored [×0.5,×2] outer box** (`max/min` against `T_phi_lo/T_phi_hi` and `anchor×[0.5,2]`), so PSO never drifts beyond a factor of 2 from the theory init in ANY stage/step while annealing is preserved. The old absolute clamps are removed; the β branch now also defines bounds for `N_TAU>1` (previously left undefined — a latent `UndefVarError` on the bins path). **Stage-0 β search** range likewise re-anchored to `prior×[0.5,2]` (N_TAU==1) so the selected `init_beta` lands inside the Stage-1 box. **Warm-start-safe**: `parallel_pso_smm` already clamps the warm-start particle to `[lb,ub]`, and every incumbent (previous best / prior-step θ̂) was itself optimized within the same anchor box, so bounds stay feasible (`lb<ub`) throughout Step1→Step3. **Backward-compatible when no prior**: `TAU_PRIOR===nothing` (fallback/all-ones init) ⇒ `phi_anchor≡0` ⇒ T box collapses to the old `[log0.5,log2]`, and β falls back to `init_beta`/incumbent × [0.5,2]. Legacy `main_pso.jl`/`tools.jl generate_halton_grid` path untouched (not on the `main.jl`/`main_gmm.jl` line). Backend-agnostic — both PSO and CMA-ES consume the same `train_stage` bounds. |
| 2026-07-08 | `load_parameters.jl`, `CLAUDE.md` | **Trade-cost-aware T starting values via a closed-form γ_ls inversion** (replaces the all-ones `T_rs_init = T_gravity./T_gravity` default). **Math**: with wages ≡ 1 the analytical sourcing map (`model_analytical.jl` `compute_moments_analytical`) factorizes as `X_ls[r,s] = T[s,r]·M[s,r]` with `M[s,r] = Σ_dr τ[r,dr]^{-θ} Ê_dr / Φ[s,dr]`, `Φ[s,dr] = Σ_{r'} T[s,r'] τ[r',dr]^{-θ}` (the destination expenditure `exp_{s,dr}` is source-region-independent, so it factors out of the r_p sum). Since `γ_ls[r,s] ∝ T[s,r]·M[s,r]`, we invert `T[s,r] ∝ γ_ls[r,s]/M[s,r]`. `M` depends on `T` only through `Φ`, so this is a cheap per-sector **fixed point** (τ fixed at the α prior throughout). The current gravity guess `T ≈ γ·w^θ` is the degenerate `τ≡1`, region-independent-`M` case — the inversion restores the trade-cost geometry (remote/high-τ regions with observed sourcing scale **up** in T). **New `load_parameters.jl` SECTION 10b** (placed after the geography precompute in SECTION 10, since it needs `LOG_DIST_DOWNSTREAM_local`; the old SECTION 7 now only computes the `T_gravity` fallback base and no longer binds the const). `_read_prior_alpha(coefs)` pulls the α prior from `stats.csv` robustly (a `prior_alpha` column, else a row whose label column == "prior_alpha" with the number in `value`; `nothing` ⇒ warn + gravity fallback). `invert_T_from_gamma(prior_alpha; max_iter=1000, tol=1e-11, damping=0.5)` builds `τ^{-θ} = exp(-θ·α·LOG_DIST_DOWNSTREAM)` (the power-law N_TAU==1 form of `build_tau` at the prior) and `Ê_dr = emp_pi_r_full` as the destination-expenditure proxy (the destination-varying term is `Y_dr`; the residual price-index factors need the GE solve and are dropped for an init), then iterates the **damped log-space** update, renormalizing to the reference region **each pass** (the map is homogeneous degree 1 in T ⇒ scale is a neutral direction that would otherwise drift/overflow). Returns `(T, iters_used)`. `T` is `(S, R_full)` with active entries positive+ref-normalized (mirrors `unpack_params`' `T[s,:] /= T[s,ref]`), inactive at 0 — masked out downstream by every `vec(permutedims(T_rs_init))[T_MASK]` consumer, so the inactive-value change (0 vs the old NaN) never enters. Non-finite/empty result ⇒ gravity fallback. **Validated in Python** (no local Julia): synthetic `γ_ls` generated from a known `T` via the same map is recovered to ~1e-10 (γ reproduced to ~1e-12) across θ∈[1,2.5], α∈[0.5,3], up to 25 regions, incl. single-active-region sectors; worst-case 220 iters (< the 1000 cap). **Init-only** — no change to `solve_network`/moments/inference/weights; only the PSO warm-start basis moves. **Diagnostic** (guarded, master-only, `try`/`catch`, never blocks estimation): after binding `T_rs_init`, saves a log-log scatter `T_init_gravity_vs_inversion_a<α>.png` (+ raw pairs `.npz`) of gravity vs γ-inversion T, **both ref-normalized**, colored by the access-weighted mean distance to markets, for α=0.5 (the requested case) and the actual prior if different. Because both inits share the same γ_ls under wages≡1, their ratio is exactly the market-access ratio `T_inv/T_grav = M(ref)/M(r)` (`M[s,r]=Σ_dr (π̂_dr/Φ_{s,dr})·d_{r,dr}^{-θα}`), functional form `≈(d_r/d_ref)^{θα}` (exact only in the single-dominant-destination limit). Prints, over d̄ ≤ 200 km, the correction distribution (min/median/max, mean|Δlog T|) and the effective OLS slope. Representative magnitude at α=0.5, θ=1 (Python, plausible ~450 km geographies): median correction ≈ 1.5–2×, up to ~2.5–3× for the most market-remote regions, ~0.5–1× for a few, mean|Δlog T| ≈ 0.4–0.6 — a systematic remoteness-correlated shift of the same order of magnitude, not a wholesale change. |
| 2026-07-05 | `test_extensive_margin.jl`, `CLAUDE.md` | **Phase-2 extension: exact union-of-boxes algorithm + reg_coef coefficient-bias gate** (still standalone/print-only, decides whether Phase 3's FKG→exact swap is worth it AND feasible). Motivated by the first G2 run: `max|D*|=22` (45/142 goods at the full `R_downstream=22` ⇒ `2^22≈4.2M`-term IE), pairwise Bonferroni returns **negative** probabilities at `|D*|=22` (useless as the hybrid's upper branch), and the per-node table sampled `u∈{0.5,0.9,0.99}` — the **low-z tail** (since `z=scale·(−log(1−u))^{−1/θ}` is *decreasing* in u) where the extensive margin is ≈0. **Three additions.** (1) **Corrected sampling** `u∈{0.02,0.1,0.3,0.6}` (low-u = high-z, where FKG bites) and a **FKG/MC ratio** column (level bias is tiny but relative bias was ~7–8× at the active nodes in the first run). (2) **Exact union-of-boxes via a dominance-pruned, memoized DAG** (`build_union_dag`/`eval_union_dag`): the true win-anywhere prob is the product-Fréchet measure of a UNION of origin-anchored boxes; using `μ(∪rest∪B)=μ(∪rest)+μ(B)−μ(∪_i(B∩B_i))` with each box a bitmask over the base `D*` columns (intersection = mask OR, `μ(box)=exp(−zinv·Σ_c qvec_c)`), boxes contained in another (`qvec` componentwise ≥) pruned at every level, and canonical pruned mask-sets memoized. Because pruning lives entirely in **z-free q-space**, the recursion STRUCTURE is z-independent ⇒ the DAG is **built once per good and evaluated per quadrature node** (the reuse Phase 3 exploits); `nsub` (distinct subproblems) is the true exact cost, and the run reports the nsub histogram + `2^|D*|/nsub` reduction to answer *does pruning tame 2^22?* Validated against the brute-force `ie_win_prob` (`assert max|union−IE|<1e-9`) and MC. (3) **reg_coef coefficient-bias gate** (`reg_coef_quad`): replicates `compute_regression_quadrature` exactly (same nodes/regressors/FE/weights) and swaps ONLY the regressand — FKG product vs the exact DAG union — then prints `α_FKG` vs `α_exact` per bin with the max relative Δ. **This is the number that actually decides Phase 3**: the FKG bias on the estimated distance coefficients, integrated over the whole productivity range (a large per-node ratio can still integrate to a small α bias, or not). Goods whose `nsub` exceeds `CAP_SUB=100_000` fall back to FKG in the coefficient pass (count reported), so the run always completes; the exact algorithm's practical feasibility is exactly what §3 measures. **Untested here** (no Julia/data); the union recursion is hand-verified on the 3-box case and guarded by the union-vs-IE and exact-vs-MC runtime gates. |
| 2026-07-04 | `tools.jl`, `test_extensive_margin.jl` (new), `CLAUDE.md` | **CMA-ES/Hessian diagnostics, Phases 1–2** (print-only + standalone; production paths byte-identical). **Phase 1 — scale-normalized identification screen** (`tools.jl` `screen_T_identification`, additive/print-only, no call-site changes — the four call sites already pass `J`/`W`/`param_labels` and discard the return): (P1a) global **normalized conditioning** `Hn = D^{-1/2} H D^{-1/2}`, `D=diag(H)` — its λ_min/λ_max/**cond printed beside the raw cond(H)**, so a large raw cond that collapses under normalization is exposed as a units artifact (Ω,A,β,T on very different scales), not weak identification; (P1b) **normalized T-block** `Hn[T,T]` conditioning, the **per-sector block spectrum** `Hn[T_s,T_s]` (sector membership from the existing `"T[sname-rname]"` label parse, now **hoisted** to a shared `col_sector` used by both the mechanism-1 support map and this block), and a **cross-sector coherence table** `‖Hn[T_s,T_s′]‖₂/√(λ_min(Hn_s)·λ_min(Hn_s′))` with its max over sector pairs — coherence ≪ 1 licenses the Phase 4–5 block preconditioning / per-sector concentration, near/above 1 flags strong inter-sector coupling. `global_eigen` gains additive fields `cond_normalized`, `T_cond_normalized`, `T_eval_min_normalized`, `block_sectors`/`block_eval_min`/`block_cond` (aligned per-sector), `coherence_max`/`coherence_argmax` (all initialized at function top so the return is safe when `J===nothing` or `T_cols` empty; existing fields unchanged). Uses `opnorm(·,2)` (spectral norm) on the off-blocks. **Gate G1**: rerun any step-2/step-4 (or `main_gmm.jl`) — expect normalized cond ≪ raw cond and coherence ≪ 1. **Phase 2 — extensive-margin geometry screen** (new standalone `test_extensive_margin.jl`, includes the model files + `load_parameters.jl` exactly like `main_gmm.jl`; prefers a saved θ̂ from `step3/theta_hat_2.npy`→`step1/theta_hat_1.npy`→`best_params.npy`, else a documented default T=T_rs_init/Ω,A=1/mild β). Measures `|D*|` (Pareto-**minimal**, least-competition destination set: `Win(dr)⊆Win(dr′) ⟺ Q[·,dr]≥Q[·,dr′]` componentwise, so the union needs only the non-dominated destinations) via dedupe + Pareto prune of the per-good competition matrix `Q[r′,dr]=T_{r′}(w_{r′}τ_{r′,dr})^{-θ}(w_pτ_{p,dr})^{θ}` (a **self-check asserts** `Σ_comp Q = −coef_dr`, matching `compute_regression_quadrature`'s `T_val − Φ_{s,dr}(w_pτ)^θ` — the r_p self-term in Φ cancels T_val), reports the `|D*|` histogram + worst-case `2^max|D*|` cost and the **G2 decision** (≤12 → exact IE everywhere; else hybrid). Then a ground-truth error table on a handful of `(s,r_p,z)` nodes: **MC** (shared competitor Fréchet draws — the true win-anywhere estimand) vs **FKG** (`1−∏(1−ρ_dr)`, the current code) vs **exact-IE** (`Σ_{∅≠S⊆D*}(−1)^{|S|+1}exp(−zinv·Φ(S))`, `Φ(S)=Σ_comp max_{dr∈S}Q`, DFS with a running max — the Phase-3 target) vs **pairwise** (IE truncated at `|S|≤2`); exact-IE must track MC to ~1/√N, and the FKG−MC gap is the **measured extensive-margin bias**. Binned-τ runs also print `|D*|` stability under β×{0.8,1.25}. **Untested here** (no Julia/data); the derivation is guarded by the coef self-check (compile-time-ish) and the MC-vs-exact-IE agreement gate at runtime. |
| 2026-07-02 | `model_analytical.jl`, `model_CP.jl`, `tools.jl`, `main.jl`, `main_gmm.jl`, `pkg.jl`, `CLAUDE.md` | Compute the **true closed-form analytical Jacobian via forward-mode AD** (`ForwardDiff`), replacing the finite-difference-of-a-closed-form path (`analytical=true` FD). **Why**: `compute_jacobian(...; analytical=true)` still took central FDs of `compute_moments_analytical` — a closed function evaluated with an arbitrary step `δ`, carrying `O(δ²)` truncation error that is worst near the γ-threshold kinks and on small-scale T columns. Forward AD gives `∂m/∂θ` at machine precision with no step, no manual GE chain-rule derivation. The analytical path (`compute_prices_analytical → compute_moments_analytical → compute_regression_quadrature`) is already generic `FT` with **no argmin/branch on a differentiated value** (unlike `solve_network`'s Ricardian argmin, which would break AD), so it is AD-ready. **AD-compat edits** (byte-identical for Float64): `unpack_params` `T_full = zeros(eltype(params), R*S)` (was `zeros(R*S)`) and `build_tau` `tau = ones(eltype(beta), …)` — the two spots that hard-coded Float64 arrays receiving param-derived values; `compute_regression_quadrature`'s final least-squares solve dispatched via new `_ls_solve(X, y)` — Float64 keeps the exact pivoted-QR `\`, Dual uses the normal equations `(X'X)\(X'y)` (a square generic-LU solve ForwardDiff differentiates; pivoted-QR least squares has no generic Dual method; identical for the full-rank `n_reg ≤ N_REG+1` design). **New in `model_analytical.jl`**: `moments_vec_analytical(params)` (the exact `vcat(vec.(m)...)[MOMENT_MASK]` assembly `compute_jacobian` uses, factored out); `analytical_jacobian_ad(theta, param_indices)` (promotes full θ to Dual, seeds only active columns → raw `∂m/∂θ`, same axes as the FD Jacobian); `validate_analytical_jacobian` (print-only gates — AD-vs-FD `max|ΔJ|`/rel gap expecting `O(δ²)`, and the **γ_ls adding-up structural test** `Σ_r ∂γ_{r,s}/∂θ_k = 0` over the FULL region set incl. the MOMENT_MASK-dropped reference row, since `Σ_r γ_{r,s} = domestic_share[s]` is a param-independent constant → AD must return ~machine zero). **`compute_jacobian` gains `analytical_ad::Bool=false` + `ad_validate::Bool=false`**: when `analytical && analytical_ad`, an early branch returns the AD Jacobian (K/base_seed/symmetry/Richardson irrelevant → `J_sd≡0`), saves the same files (`jacobian*.npy`, `_elasticity`, `_sd`, `_elasticity_sd`, `_param_indices`), and (if `ad_validate`) computes the FD Jacobian once for the cross-check; **same return shape ⇒ drop-in**. **Wired on**: `main_gmm.jl` production GMM inference Jacobians at θ̂_1 (step2) and θ̂_2 (step4) → exact SEs (`gmm_note.txt` updated: "forward-mode AD" not "single FD replication"); `main.jl` both 2×2-test `J_ana` sites → the Jacobian axis of `run_2x2_inference_test` becomes a clean noisy-sim vs exact-closed-form contrast (whole axis-1 gap = Ricardian simulation noise in `J_sim_gb`, no FD artefact). `ad_validate=true` at all four sites. `ForwardDiff` added to `pkg.jl`. **Untested here** (no Julia/data); residual risk is the Dual `\` in reg_coef (handled via the normal-equations fallback) — the γ adding-up gate is the runtime check. Also resolved pre-existing CLAUDE.md merge-conflict markers (kept both 2026-06-19 rows). |
| 2026-06-24 | `model_CP.jl`, `main.jl`, `main_gmm.jl`, `load_parameters.jl`, `run.sh`, `CLAUDE.md` | Add a **selectable `:sobol` draw method** (per-sector digitally-shifted Sobol net, FLAT weights, deterministic under `randomise=false`) and validate it inside the existing `test_price_alignment` harness — no throwaway script. **`model_CP.jl`**: new `sobol_scrambled_net(N, d, rng)` builds the deterministic Sobol net via `QuasiMonteCarlo.sample(N, zeros(d), ones(d), SobolSample())` (the proven 4-arg call form already used in `tools.jl`), then applies a **base-2 digital shift** (XOR of the 52-bit fixed-point coordinates with a per-dimension mask drawn from the EXPLICIT `rng`). Digital shift, not QuasiMonteCarlo's `OwenScramble`, **because** the requirement is an rng-threadable randomisation reproducible under a frozen seed without ever calling `Random.seed!` globally — QMC's scramble draws from the global RNG and can't be seeded per call; the digital shift preserves the (t,m,s)-net equidistribution and dissolves the all-zeros Sobol origin (no coordinate → u=0). `generate_sobol_draws(N_rho, n_good; randomise, rng, seed=42)` builds ONE net per sector of dimension `d = length(SECTOR_GOOD_INDICES[s])` (keeps the QMC dimension low where the Ricardian `min_r c_r` interaction lives — within a sector across its regions), scrambling each from a master = `MersenneTwister(seed)` when `randomise=false` (frozen, PSO-safe — a Sobol net at a fixed seed, **not** a midpoint rule) or the supplied `rng` when `randomise=true` (independent designs per Σ_sim replication); columns assigned to `SECTOR_GOOD_INDICES[s]`, `@assert covered == n_good`, **flat** `W = 1/N_rho` (so `solve_network`'s winner-weight shortcut stays exact ⇒ unbiased for the min-coupled moments, same property as `:qmc`). Dispatcher `generate_draws` gains the `:sobol` branch; docstring + error message updated. **`MersenneTwister` master rather than the plan's `Xoshiro`** — matches the codebase idiom (`generate_qmc_draws`/`generate_is_draws` use `MersenneTwister`), no version risk. The `(N_rho × n_good)` weight-matrix invariant is preserved, so every `[rho, g]` consumer, `MOMENT_MASK`, and inference are untouched. **Asserts**: `:sobol` added to the `draw_method` whitelist in `main.jl`, `main_gmm.jl`, `load_parameters.jl §9`, and the `run.sh` launcher (else `run.sh --draws=sobol` would be rejected before Julia starts). **`main_gmm.jl test_price_alignment`** (the non-bias gate, free): added the `:sobol` arm to the closed-form comparison loop (inherits the per-block error table) — expected to align like `:qmc` on all blocks, unlike biased `:is`; added an **intra-sector decorrelation gate** (`max|cor(U[:,cols])|` on the 3 thickest sectors, expected ≈ 1/√N — catches a broken scramble that the global `max|cor|` would miss); switched `N_list` to **powers of 2** `[2048, 8192, 32768]` (Sobol's (t,m,s)-net variance property is sharp at N=2^m). **New `test_sobol_variance(params; K=64, N=8192)`** (the qmc-vs-sobol DECISION gate): asserts `randomise=false` reproducibility, then runs K `randomise=true` replications via `pmap` (`_, m = full_SMM(params; u_draws, sample_weights)` — note `simulation=true` returns the X_ls trade matrix, NOT moments, so the loss-returning default form is used) and compares mean γ-block variance, reporting `ratio = Var_sobol/Var_qmc` (≤ 1 expected on thick sectors). **Additive/test-only beyond the generator + asserts**: no change to `solve_network`, weights, `U_DRAWS` construction, or inference; production paths with the default `:qmc` are byte-identical. **Untested in this environment** (no Julia/data); the genuinely uncertain point is the digital-shift caveat — it is a digital shift, not a full Owen nested scramble (origin dissolved, net preserved; intra-sector gate is the runtime check). |
| 2026-06-20 | `model_CP.jl`, `load_parameters.jl`, `CLAUDE.md`, `claude.md` | Remove the dead `second_stage` framework and reorganize `load_parameters.jl` for readability. **Dead-code removal** (verified dead first): the `second_stage=true` branch of `full_SMM` (`model_CP.jl`) referenced `empirical_moments_reduced` / `Weight_matrix` / `mask_emp_gamma_ls`, which are defined **only** in `run_amplification_analysis.jl` (and that file includes `model_CP.jl`/`tools.jl` but **not** `load_parameters.jl`, and never calls `full_SMM`); every live caller in `main.jl`, `main_gmm.jl`, `tools.jl`, and `pso_integration.jl` passes `second_stage=false` (or the default). The branch is collapsed to its `else` body. **Signature decision**: the `second_stage` positional arg is **kept as an ignored/defaulted parameter** rather than removed — `parallel_SMM` (`tools.jl:235`) forwards it positionally to `full_SMM`, and it threads positionally through `parallel_SMM_safe` ← `train_stage_one` ← `compute_scores_modular` plus `pso_integration.jl:475`, so deleting it would ripple a positional-arg refactor across four call layers (the lower-risk fallback the task flags). `main_pso.jl` is **untouched** (legacy, byte-identical; passes `second_stage=false`). In `load_parameters.jl`: removed the now-orphaned `@everywhere const Weight_matrix = nothing` (only the dead branch read it), the dead `N_rs_local = NPZ.npzread("N_rs.npy")` load (never used), and the duplicate `n_reg = length(reg_coef_local)` recomputation (kept the earlier one feeding the `N_REG` broadcast). The `w_rs`/`regional_wages` collapse-to-1 normalization is deliberately **left in place** (not a no-op). **Reorganization**: `load_parameters.jl` is grouped into 13 banner-delimited sections (raw data → calibration → gamma-threshold pruning → T-mask/index maps → reference regions → remaining targets + N_REG/N_TAU → T gravity start → moment vector/MOMENT_MASK/BLOCK_RANGES → draws → geography precompute → identified-param indices → γ ref-region map → labels) with one-line "why" comments per section. **Exact execution order, every constant value, and all `@everywhere` broadcasts are unchanged** — purely additive comments plus the four dead-code deletions; the post-include contract (`N_moments`, `n_good_local`, `jacobian_param_indices`, `BLOCK_RANGES_local`, all consts) is preserved. Not byte-compatible only in that the dead `Weight_matrix` const no longer exists. |
| 2026-06-19 | `model_CP.jl`, `load_parameters.jl`, `tools.jl`, `model_analytical.jl`, `main.jl`, `main_gmm.jl`, `run.sh`, `claude.md`, `CLAUDE.md` | Make the Fréchet draw sampler **selectable** and change the default from importance sampling (`:is`) to stratified-uniform QMC (`:qmc`). **Why**: `:is` is **biased** for the min-coupled consumer moments (`c_tilde`, `gamma_ls`, `industry`, `pi_r`). `solve_network`'s CES price index applies only the WINNING column's weight `W[rho, winner_good_idx[rho,s]]` per row and drops the losing columns' importance-density ratios; with a non-flat tilt those ratios do not cancel, so the realised per-row weight is not the correct IS weight for the joint (min over regions) functional — a bias that does not vanish as `N→∞`. The prior "bounded weights ⇒ no degeneracy" guarantee only makes `:is` unbiased for **single-column** functionals (the `reg_coef` quadrature rows, no min-coupling), where the `u→0` tilt is still valuable. **Design** (`model_CP.jl`): renamed the IS body to `generate_is_draws` (kept `a`/`verbose`); added `generate_qmc_draws` (stratified uniform, independent `randperm` per column → decorrelated, midpoint/jitter modes, **flat** weight matrix `1/N_rho` → winner-weight shortcut exact); kept `generate_mc_draws`; added dispatcher `generate_draws(N_rho, n_good, method::Symbol; randomise, rng, a, verbose)` for `method ∈ {:qmc,:mc,:is}`; `generate_stratified_draws` is now a thin **alias → `:qmc`** so legacy call sites compile unchanged. **The weight stays an `(N_rho × n_good)` matrix for every method** — this is the invariant that confines the switch to the generation sites; consumer indexing (`solve_network` CES/`exp_val`, `fast_weighted_regression`), `MOMENT_MASK`, T flat-indexing, weight-matrix construction, and inference are all untouched. **Threading**: entry points (`main.jl`/`main_gmm.jl`) parse `draw_method` (5th positional arg, default `:qmc`); `load_parameters.jl` broadcasts `@everywhere const DRAW_METHOD` (via `QuoteNode`) and builds `U_DRAWS`/`SAMPLE_WEIGHTS` with it; `build_step3_weight_matrix` (Σ_sim), `compute_jacobian` (replication draws), and `test_analytical_vs_simulated` gain a `draw_method::Symbol=DRAW_METHOD` kwarg forwarded to `generate_draws`. `run.sh` surfaces `--draws=qmc|mc|is` (passes K_sim=10000 for SMM so draws land at arg 5). **`:mc` determinism**: `randomise=false` ⇒ `MersenneTwister(0)` (PSO-safe). **`:qmc` Σ_sim caveat**: with `randomise=true` all columns of one call share the single replication `rng` stream (decorrelated via distinct perms, but not independent `MersenneTwister(g)` streams as in the base mode) — intentional, Σ_sim wants independent designs per replication. **`test_price_alignment` rewritten**: drops the `:is`-specific `strat_flip`/`(1-u)^{1-a}` arms; now compares `{:qmc,:mc,:is}` vs the closed form. Expected at θ̂_2: `:qmc ≤ :mc` on the min-coupled blocks, `:is` visibly biased there (documents, not hides, the defect), `reg_coef ~1e2` for all three (FKG/quadrature, not sampling). **NOT byte-compatible** with the `:is` estimator — that was targeting a biased object; `:is` is demoted, not removed (still the consistent sampler for `reg_coef`). |
| 2026-06-19 | `run_internal_validity.jl` (new), `CLAUDE.md` | Add a standalone **internal-validity Monte-Carlo driver**. **Additive only** — no existing file is modified, so every production entry point (`main.jl`, `main_gmm.jl`) is byte-identical. It builds a synthetic truth `θ₀ = [agg_labor_share \| agg_industry_share \| A_gravity \| β₀ \| vec(permutedims(T_rs_init))[T_MASK]]` (A_gravity = Stage-0 analytical inversion; β₀ CLI-supplied, asserted monotone), then **overrides only the targets**: after `include("load_parameters.jl")` (so all masks / `T_REF_REGION` / `MOMENT_MASK` / `gamma_threshold` are frozen from the REAL baseline), `set_synthetic_targets!` mutates the const arrays `empirical_moments .= reshape(masked m(θ₀),1,:)` and `reg_coef .= m(θ₀)[BLOCK_RANGES[4]]` **in place** across master+workers (`@everywhere`, no const rebinding → compiled `full_SMM`/Stage-0 see new targets, no staleness). `emp_gamma_ls`/`X_rs`/`emp_pi_r_full` stay REAL. m(θ₀) is generated with a SEPARATE draw seed (`generate_stratified_draws(...; randomise=true, rng=MersenneTwister(999_000))`) so the estimator's production `U_DRAWS` never match the target-generating draws. **Section 1** records `min_g ESS_g` (production draws) and the `H[T,T]` λ_min via `screen_T_identification(θ₀; J=…)`. **Experiment 1** (full-vector point recovery): 10 perturbed starts (×LogUniform[0.5,2] on A,β,T; ×U[0.8,1.2] on Ωᴸ,Ωˢ; β re-sorted), each run through the full three-step (`run_pso_optimization` step1 `skip_initial_beta_search=true,warm_start_params=pert` → `build_step3_weight_matrix` → step3 `gamma_beta_only=true` → step4 `compute_jacobian`+`compute_smm_inference`), reporting `max\|id(θ̂)−id(θ₀)\|/\|id₀\|` and `\|Δ\|/SE` on β+T plus cross-start dispersion. **Experiment 2** (β+T coverage, **M=200**, light-polish PSO `n_particles=40,max_iter_init=60,max_iter_stage=30,max_loop=3`): W/Ω built ONCE at θ₀ (fixed across reps), then per rep draws `m_b = m(θ₀)+ε_b`, `ε_b~N(0,Σ_data)` on the β+γ block (`Σ_data` PSD-sqrt'd), re-estimates β+T warm-started at θ₀, per-rep `compute_jacobian`+inference; reports per-param coverage / bias / `SD(θ̂)/mean(SE)`. **Gauge handling (critical)**: inference SEs are RAW, but raw `T` has an unidentified per-sector reference scale (`unpack_params` divides `T[s,:]` by `T[s,ref]`), so coverage is done in the **T_ref=1 gauge** — point estimates taken straight from `identified(·)` (already normalized), raw SEs rescaled per param by `gauge_factors(θ)` (= 1 for β, = `θ[ref-raw-index(sector)]` for T). Index helpers (`BETA_T_START`, `T_PARAM_OFFSET`, `GB_COLS`/`GB_PARAM_IDX`/`GB_INDICES`) mirror `main.jl`'s β+γ/β+T slicing exactly. Per-experiment `output_folder` is rebound (global) so `run_pso_optimization`/`generate_report` write to per-start/per-rep trees under `internal_validity_<industry>/`. Soft-scope `global` declarations added where the top-level `for` loops rebind globals (`min_ess`, `output_folder`). A `SMOKE_TEST` flag shrinks M/PSO for an end-to-end wiring check. **Untested in this environment** (no Julia/data present); authored to mirror `main.jl` call patterns — run SMOKE_TEST first. |
| 2026-06-18 | `model_CP.jl`, `CLAUDE.md` | Fix `solve_network`/`fast_weighted_regression` to use the **passed draw count** (`N_rho_eff = size(u_draws,1)`), not the global `N_rho` const. **Bug**: every per-variety loop/array used the global `const N_rho = 8000` while the weights were self-normalised over the *passed* number of draws. When a caller passes draws with more rows than `N_rho` (e.g. `test_price_alignment`, which sweeps `N_list=[8000,32000,128000]` but cannot change the `const`), the solver consumed only the first `N_rho` rows yet the per-column weights summed to `1` over all `N`, so the realised CES weight sum was `N_rho/N < 1`. The within-sector price index `P_sr = [Σ_ρ w_ρ p_ρ^{1-ν_s}]^{1/(1-ν_s)}` (`1/(1-ν_s) = -2` at `ν_s=1.5`) was therefore inflated by `(N/N_rho)^2`, blowing up `c_tilde` (hence `labor`, `pi_r`) for **all** samplers — the price-alignment test showed `c_tilde` error 5e-3 → 11.5 → 187 across the three `N` (ratios ≈ `(N/8000)^2`), while normalised-share moments (`gamma_ls`, `industry`) stayed flat. **Fix**: `solve_network` derives `N_rho_eff` from `u_draws` (else `sample_weights`, else global `N_rho`), asserts `u_draws`/`sample_weights` row counts agree, and sizes/loops all per-variety arrays (`z_flat`, `linkages_flat`, `p_rho_s`, `winner_good_idx`, `w_rho_s`, the `exp_val` accumulation) by `N_rho_eff`; `fast_weighted_regression` uses `N_rho_eff = size(sample_weights,1)` for `N_valid` and its `rho` loop. **Production-invariant**: every production caller (`load_parameters`, `main_pso`, `build_step3_weight_matrix`, `compute_jacobian`) generates draws at the global `N_rho`, so `N_rho_eff == N_rho` and behaviour is byte-identical; only off-size callers (the N-sweeping test) are corrected. Pre-existing coupling, not introduced by the IS refactor. |
| 2026-06-18 | `model_CP.jl`, `tools.jl`, `pso_integration.jl`, `main.jl`, `main_gmm.jl`, `CLAUDE.md`, `claude.md` | Replace comonotonic stratified-QMC draws with **per-column independent importance sampling**. `generate_stratified_draws` now returns an `(N_rho × n_good)` weight **matrix** `W` (was a length-`N_rho` vector); each good pair `g` gets an INDEPENDENT proposal stream, so `u[rho, g1] ⊥ u[rho, g2]`. The old shared 25-bin grid made every region's productivity a near-deterministic function of one latent per row, collapsing Ricardian selection (the `min_r c_r` winner became fixed by `T/tau/w`, not by independent Fréchet draws) — symptom: price-alignment `strat` columns ~100× worse than `mc`. **New design**: in uniform space, proposal `q(u)=a·u^{a-1}` (Beta(a,1), inverse-CDF `u=v^{1/a}`) oversamples the selection-relevant `u→0` tail; target Uniform ⇒ IS weight `w ∝ 1/q(u)=u^{1-a}/a`, **bounded on [0,1]** (no degeneracy, unlike a Fréchet/z-space tilt). Proposal uniform `v` is stratified (one point per equal-prob stratum) with an **independent `randperm` per column** → decorrelated AND low-variance. `randomise=false` → `MersenneTwister(g)`+midpoint (PSO-deterministic); `randomise=true` → supplied `rng` perm+jitter (Σ_sim). New kwargs `a::Float64=0.5` (tilt; smaller ⇒ heavier tail, lower ESS) and `verbose::Bool=false` (prints `min_g ESS_g`). Weights self-normalised per column (`Σ_rho W[rho,g]=1`). **Consumer threading** (the gate — every `[rho]` weight access becomes `[rho, winning-g]`): `solve_network` CES `P_sr` now weights each `(rho,s)` variety by `sample_weights[rho, winner_good_idx[rho,s]]` (per-`(rho,s)` matrix `w_rho_s`, replacing the shared `reshape(sample_weights,N_rho,1)`); `exp_val` uses `sample_weights[rho, g_winner]`; backward-compat branch builds `fill(1/N_rho, N_rho, n_good)`; `fast_weighted_regression` uses `sample_weights[rho, g]` (param typed `Matrix{Float64}`); `main.jl` firm-level export uses `[rho, g]`. `sample_weights` kwarg type `Vector{Float64}`→`Matrix{Float64}` across `solve_network`/`SMM`/`SMM_with_network`/`full_SMM`/`parallel_SMM`/`parallel_SMM_safe`/`train_stage_one`/`generate_report`/`compute_scores_modular`/`run_reporting`/`train_stage_pso`. `test_price_alignment` now recomputes the `strat_flip` arm's weights from the flipped quantile `(1-u)^{1-a}` (reusing un-flipped `ws` is an invalid SNIS pairing) and reports `min ESS` + `max|cor(U)−I|` per `N`. **Not byte-compatible** — the estimator now targets the correct (decorrelated) object; the old behaviour was biased and there is **no recovery flag** (intentional). `reg_coef` block error is *unaffected* (separate FKG/quadrature bias). **Caution**: the tilt is keyed to the `−log(1−u)` transform (high-z mass at `u→0`); do not flip the draw without recomputing the weight from the flipped quantile. `using Printf` added to `model_CP.jl` for the ESS line. |
| 2026-06-15 | `tools.jl`, `CLAUDE.md` | Extend `screen_T_identification` with **three print-only mechanism-attribution diagnostics** for a small `H[T,T]` λ_min, all computed inside the existing `if J !== nothing` block from the already-built `H`, `T_cols`, `J`, `W`, `param_labels` (no new call-site wiring — call sites already pass these). **(1) Global v_min support**: the smallest-eigenvalue eigenvector `v_min` of `Symmetric(H[T_cols,T_cols])` is mapped to sectors by parsing each T column's `"T[sname-rname]"` label (strip `"T["`/`"]"`, split on the LAST `-`, sector before — same logic as `test_t_reorder.jl`; `sname_to_s` from `_sector_names`), printing per sector the squared-mass share `Σ v_min[sector]²/Σ v_min²` and dominant sign `sign(Σ v_min[sector])`, and flagging CONCENTRATED (one sector > 0.8 mass → **mechanism 3**, level-weak sector) vs SPREAD with mixed signs (→ **mechanism 1**, inter-sectoral flat direction). **(2) M^s cross-check**: after the per-sector analytical screen builds `out`, prints per sector the v_min support/sign beside that sector's M^s ratio `eval_min/eval_max`, so the worst-M^s sector can be matched to where the global weak direction concentrates. **(3) W=I vs W=W_eff**: recomputes `H_I = J'J` and its `Symmetric(H_I[T_cols,T_cols])` λ_min/conditioning beside the W-weighted values — weak under W_eff but well-conditioned under I ⇒ **mechanism 2** (the W metric de-identifies the T-via-γ_ls params); weak under both ⇒ intrinsic to J. **Returned `global_eigen` gains additive fields** `T_evec_support::Vector{Float64}`, `T_evec_signs::Vector{Int}`, `T_evec_sectors::Vector{Int}`, `T_eval_min_identity::Float64` (initialised at function top so they are always defined, incl. when `J===nothing`); existing fields (`eval_min/eval_max/cond/rank/T_eval_min/T_eval_max`) and all four call sites (which discard the return value) are unchanged. Respects the β-then-T gb column order (existing contiguous-suffix assert untouched); eigen on `Symmetric` wrappers; cost trivial. **Print-only** — no estimate, weight matrix, inference output, or file is affected. |
| 2026-06-15 | `tools.jl`, `claude.md`, `CLAUDE.md` | Make `compute_jacobian` robust to low-magnitude `T_{sr}` columns via a scale-invariant **log-space central step** with explicit raw-unit back-conversion. **Problem**: T columns previously used the same additive central step `h = max(|θ|·step_rel, step_abs)` as every other parameter. Below `|θ|≈1e-5` the absolute floor (`step_abs`, e.g. `1e-9` vs `step_rel=1e-4` relative) broke step-to-magnitude proportionality, and a minus step could straddle the Fréchet `max(T_sr, eps)^(1/θ)` clamp — silently turning the central difference into a biased one-sided difference. Both corrupt the T Jacobian column and hence the T parameter SEs. **Fix** (one function, `compute_jacobian`): columns are partitioned by flat parameter position — **T columns** (strictly-positive, multiplicatively-entering; flat index `≥ 1 + S + R_downstream + N_TAU + 1`, layout `[Ω^L(1) | Ω^s(S) | A(R_downstream) | β(N_TAU) | T]`) now perturb at `θ·exp(±δ)` with `δ = step_rel`, and the log-space central difference `(m₊−m₋)/(2δ)` is converted **back to raw units** by dividing by `θ_j`: `J[:,k] = (m₊−m₋)/(2δθ_j)` (chain rule `∂m/∂θ = (∂m/∂lnθ)·(1/θ)`). The log step is scale-invariant (never crosses zero, immune to the floor, never hits the `eps` clamp). **All other columns** (`Ω^L`, `Ω^s`, `A`, `β`/`α`) keep the additive step unchanged — they are not uniformly multiplicative/sign-definite (e.g. `τ = 1+β_b`), where additive is correct. **The stored column is raw `∂m/∂θ` in both regimes**, so `G = ∂m/∂θ` and `Var = (G'WG)^{-1}` are unaffected in units; the log step is purely a numerical-accuracy device. `J_elast` is still derived from raw `J`. **New kwargs** (backward-compatible defaults): `t_log_step::Bool=true` (set `false` → byte-identical old additive behaviour for every column), `check_symmetry::Bool=false` (opt-in print of per-T-column forward-vs-backward asymmetry, flagging entries diverging by >10× across-replication `J_sd`, i.e. nonlinear/clamped regime), `richardson_check::Bool=false` (opt-in recompute of T columns at `2δ`, reporting the relative gap to the `δ` estimate). Both diagnostics are **print-only** and never alter returned `J`. A `@assert θ_j > 0` guards the log set (T_MASK already excludes zeros → should never fire); the non-finite-Jacobian error now names each offending column's flat param index and step regime. The analytical branch (`analytical=true, K=1`, used by `main_gmm.jl`) routes T columns through the same per-column log logic. **No call site changes** — all callers in `main.jl`, `main_gmm.jl`, and the 2×2 test pass only `theta`/`param_indices`/steps/seeds, all unchanged; the moment vector, `MOMENT_MASK`, and `jacobian_param_indices` are untouched. The `unpack_params` normalization `T_mat[s,:] ./= T_mat[s,ref]` is **deliberately left in place** — the chain rule absorbs it automatically since both perturbed evaluations pass through the same `unpack_params`; no perturbation in normalized space. **Expected diff**: T-block entries of `se_theta.npy` / `se_theta_sandwich.npy` will shift slightly (different truncation error — the intended correction, better-conditioned columns); β/α, Ω, A SEs are invariant. |
| 2026-06-15 | `tools.jl`, `main.jl`, `main_gmm.jl`, `CLAUDE.md` | Promote `screen_T_identification` to a shared `tools.jl` diagnostic and wire it after **every** Jacobian computation in both entry points (4 call sites). **Pre-fix** (required — `main.jl` failed to load): the `compute_jacobian` call at θ̂_2 in `run_step4` passed the `step_rel` kwarg twice (`1e-4` then `1e-9`), a duplicate-keyword parse error; the second occurrence is now `step_abs = 1e-9` (no other change to that call). **Move + extend**: the per-sector analytical eigen-screen (`M^s = Σ_dr ω_dr (diag g − g g')` on free non-ref regions; smallest eigenpair + marginal shares + curvature) was lifted out of `main.jl` into `tools.jl` — its `compute_prices_analytical` dependency is satisfied because both entry points include `model_analytical.jl` before any tools usage (function globals resolve at call time, after `load_parameters.jl`). New keyword signature `screen_T_identification(params; J=nothing, W=nothing, param_labels=nothing, label="")`: when `J` (the β+γ rows × β+T cols `J_gb` slice) is supplied it first prints, prefixed by `label`, (a) the global GMM-information eigen-screen `H = Symmetric(J' W J)` (λ_min, λ_max, cond, rank(J) via `svdvals > sv[1]·1e-8`; `W===nothing` ⇒ identity), then (b) the T-only sub-block `H[T_cols,T_cols]` λ_min/λ_max where T columns are those whose `param_labels` start with `"T["` (fallback `N_TAU+1:end`; asserts β/α-first-then-T column order when labels present; guards empty `T_cols`); then (c) the unchanged per-sector screen. Returns `(out, global_eigen)` — the original per-sector NamedTuple vector plus a `global_eigen` summary NamedTuple (`nothing` when `J===nothing`). **Call sites** (all immediately after Jacobian + gb-slicing, inside the relevant `run_step` guard): `main.jl` step2 `J_gb`/`Weight_matrix_custom[gb,gb]`, step4 `J2_gb`/`W_step3`; `main_gmm.jl` step2 `J1_gb`/`W_step1`, step4 `J2_gb`/`W_eff`. The two old bare calls `screen = screen_T_identification(theta_hat_*)` (which ran *outside* any step guard in `main.jl`) are removed. **Pure diagnostic**: print-only (stdout), no new files, no change to any estimate/weight/inference output; eigen on `Symmetric` wrappers, cost trivial (`H` ≤ (N_TAU+n_T)²). |
| 2026-06-11 | `tools.jl`, `main_gmm.jl`, `CLAUDE.md` | Fix GMM Step-2 Σ_data threshold misalignment by sharing the SMM's reconciliation logic. The SMM (`build_step3_weight_matrix`) reconciles the on-disk `Sigma_beta_gamma[_1].npy` against the (possibly gamma-thresholded) active set — dropping β+γ rows/cols for (s,r) pairs pruned by `gamma_threshold` — but `main_gmm.jl` Step 2 took a **naive positional slice** `Sigma_data_full[1:N_gb, 1:N_gb]` with no `T_MASK` filter. When the file is pre-threshold (size `n_gb_old > N_gb`), that slice silently mixes surviving and pruned γ moments, so `W_eff` is built on a covariance misaligned with the threshold-aware Jacobian/moment vectors (`J2_gb`, `emp_vec_gb`); the existing count-only `@assert size(Omega_gmm,1)==N_REG+length(BLOCK_RANGES[5])` couldn't catch it (right count, wrong rows). With `gamma_threshold=0.01` (nonzero default), this is a live risk, not latent. **Fix**: extracted the SMM's reconcile block (the `X_rs.npy` → `T_mask_moment_old` → reference-region removal → `gamma_old_positions`/`survive`/`keep_idx` → three-way size branch `==n_gb` use-as-is / `==n_gb_old` subset / else error) into a shared `tools.jl` helper `reconcile_sigma_data(Sigma_full, input_folder)`; `build_step3_weight_matrix` now calls it (SMM behavior **bit-identical**), and `main_gmm.jl:150` replaces the naive slice with the same call (the `w_beta`/`w_gamma` block-diagonal fallback flows through it too). GMM thereby gains a guaranteed `X_rs.npy` dependency — already read by `load_parameters.jl:30` (included before Step 2), so safe. Step 4's reload of `step2/Sigma_data_gb.npy` inherits the fix. The subset branch (with its documented `c_s` renormalization caveat → γ SEs ~`c_s` too tight) is **preserved** so GMM matches SMM exactly rather than diverging; the stricter "refuse to subset, force regeneration" policy for the exact GMM path remains an opt-in not taken here. |
| 2026-06-11 | `tools.jl`, `main.jl`, `CLAUDE.md` | Add a **test-only** 2×2 noise-decomposition diagnostic, `run_2x2_inference_test` (tools.jl), gated behind a single `run_2x2_test = false` flag in `main.jl` (default **OFF** → behavior byte-identical to before). It crosses two axes at a fixed estimate θ̂: axis 1 (Jacobian) {simulated FD `J_sim_gb`, analytical FD `J_ana_gb`}, axis 2 (W,Ω) {data-only `(Σ_data, Σ_data⁻¹)`, data+sim `(Ω=Σ_data+Σ_sim, W=Ω⁻¹≈W_step3)`}; the (analytical, data-only) corner is the GMM-style variance at the SMM estimate, to attribute SMM parameter-variance noise to the Jacobian vs the weighting channel. The function is **fully standalone**: it replicates ONLY the linear-algebra core of `compute_smm_inference` inline (GtWG with cholesky→eigenvalue-floor fallback, efficient + sandwich variance, `se_sw`, Hansen J=r'Wr, df, p, rank(G)), with its own symmetrize/PD guards, and does **NOT** call `compute_smm_inference`. Each of the four cells is wrapped in try/catch (failed cell → NaNs, continue). **Hard isolation**: it does not modify `compute_smm_inference`, `model_analytical.jl`, `load_parameters.jl`, or `main_gmm.jl`; it does not touch the production inference calls or their `step{2,3}/inference/` outputs. All test outputs go under a separate tree `<step>/inference_2x2_test/`: a distinct report `report_2x2_<label>.txt` (its own format — 2×2 tables per metric {mean se_sw, median se_sw, Hansen p, rank(G)}, a channel-decomposition block, a per-parameter four-cell se_sw table, plus the verbatim validity note) and `se_2x2_<label>.npy` (Dict of the four se_sw vectors for external plotting; NO plotting inside the function). Call sites: at θ̂_1 (run_step2) and θ̂_2 (run_step4), AFTER the existing inference call, reusing the already-built simulated gb Jacobian (`J_gb`/`J2_gb`) and computing only the analytical gb Jacobian via `compute_jacobian(...; analytical=true, K=1, base_seed=3_000_000`/`4_000_000)` (fresh seeds, never colliding with existing Jacobian/Σ_sim seeds). `gamma_ref_map`/`gb_block_ranges`/`gb_block_names` accepted for signature parity but unused (no per-block/γ-ref plots). New `n_quad=200` flag added near arg-parsing. |
| 2026-06-10 | `tools.jl`, `CLAUDE.md` | Add reference-region γ points to `generate_report`'s dashboard `p1` panel, mirroring `compute_smm_inference` §6b (`gamma_ref_map` block). Previously `p1` plotted only `BLOCK_RANGES[5]` (active, non-reference γ), so each sector's MOMENT_MASK-dropped reference region was missing. `p1` is now built **directly** in `generate_report` (option b) instead of via `bubble_scatter`, because the legend + two-series color/marker distinction are p1-specific; labelling `bubble_scatter`'s series to drive a legend would spill it onto the shared p2/p3 panels. `bubble_scatter` and p2/p3 are unchanged. Per sector in `GAMMA_REF_MAP`, the reference point is reconstructed from the adding-up constraint `y_ref = c_s − Σ sim_gamma[local_positions]`, `x_ref = emp_ref` (skip when `emp_ref ≤ 0`, `local_positions` empty, or `max(local_positions) > length(sim_gamma)`). **Zero offset**: `sim_gamma` is the γ block as a standalone 1-based vector, so `local_positions` index it directly — NO β-offset (unlike §6b's β-then-γ subsystem). Styling matches §6b: non-reference = `RGB(0.247,0.404,0.667)` circles ("Non-reference"), reference = `RGB(0.75,0.30,0.20)` `:diamond` ("Reference (reconstructed)"); axis limits span both series; legend at `:bottomright`. The WLS coef/t-stat annotation is computed on **non-reference points only** — reconstructed reference points do not enter the fit. No error bars (generate_report has no fitted-moment SEs, unlike §6b). |
| 2026-06-10 | `load_parameters.jl`, `model_CP.jl`, `tools.jl`, `pso_integration.jl`, `main_pso.jl`, `test_t_reorder.jl`, `CLAUDE.md` | Unify the T-parameter flattening to **s-major** (`vec(permutedims(X_rs))`), identical to the γ-moment and `T_MASK_MOMENT` convention, so the Jacobian's parameter axis aligns column-for-column with its γ-moment row axis (`T[s,r]` column ↔ `γ[s,r]` row). Previously T parameters used region-major `vec(X_rs)` while γ moments used s-major — the two Jacobian axes disagreed. The parameter LAYOUT `[Ω^L \| Ω^s \| A \| β \| T]` is unchanged; only the internal order of the active-T sub-block changes. Coupled edits (all move together; partial application silently transposes `T_mat`): (1) `load_parameters.jl` `T_MASK = vec(permutedims(X_rs)) .> 0` (now == `T_MASK_MOMENT`); (2) `good_indices = findall(permutedims(reshape(T_MASK, R, S)))`; (3) `model_CP.unpack_params` inverts the new flatten: `T_full = zeros(R*S); T_full[T_MASK]=T_reduced; T_mat = permutedims(reshape(T_full, R, S))` (still returns region-major `vec(T_mat)`, so inter-stage full-T is unchanged); (4) jacobian ref-exclusion `flat_pos = (s-1)*R_full + ref_r`; (5) `PARAM_LABELS` T loop `s=((fp-1)÷R_full)+1, r=((fp-1)%R_full)+1`. Phase-0e surfaced **4 additional active flat-T consumers beyond the documented 5** (the "five edits" premise undercounted): `tools.jl:build_step3_weight_matrix` Σ_data subset (`reshape(collect(T_MASK),S,R)` → `collect(T_MASK)`, used by main.jl/main_gmm.jl Step-3 inference); `tools.jl` & `pso_integration.jl` PSO init/bounds (`vec(T_rs_init)[T_MASK]` → `vec(permutedims(T_rs_init))[T_MASK]`); `pso_integration.jl` warm-start reduce (`params_dict[:T][T_MASK]` → `vec(permutedims(reshape(params_dict[:T],S,R)))[T_MASK]`); and `main_pso.jl` (shares `unpack_params`, so its own region-major `T_MASK`/`good_indices`/`T_init` were flipped too, else it silently breaks). The dormant `second_stage` masking path (`mask_emp_gamma_ls`, `second_stage=false` in every main flow; only `run_amplification_analysis.jl` defines the mask) is intentionally left untouched. `GOOD_S/GOOD_R` remain region-major (findall over (S,R)) — confirmed nothing positionally aligns `GOOD[g]` with the reduced-T block (model reads `T_mat[s,r]` via `SR_TO_GOOD`; all param-block construction uses `T_MASK` order). New `test_t_reorder.jl` GATE asserts T1 (`T_MASK==T_MASK_MOMENT`), T2 (unpack round-trip), T3 (label↔index), T4 (γ-row order == T-col order). |
| 2026-06-09 | `tools.jl` | Fix field-mismatch in `generate_report`: `gamma_emp_result`/`gamma_sim_result` were built with `matrix_report(..., false)` (4-field tuple), but `generate_dashboard_report` accesses `.n_zeros` expecting the 5-field form. Restored to `matrix_report(..., true)` for γ only; π_r remains `false` (zero counts uninformative there, γ sparsity is a meaningful diagnostic). Added a one-line comment to `matrix_report` documenting the asymmetric return type. |
| 2026-06-06 | `main_gmm.jl`, `CLAUDE.md` | Restructure `main_gmm.jl` to mirror `main.jl` control flow. Introduces `run_step1..run_step4` booleans and a `step2/W_eff.npy` resume guard (parity with `main.jl`'s `W_step3.npy` guard). Fixes a guaranteed `UndefVarError`: `Omega_gmm = Sigma_data_gb` was assigned *after* the `@assert size(Omega_gmm,…)` check — assignment now precedes both correctness asserts. Adds θ̂_1 inference in Step 2 (into `step2/inference/`, `K_sim=0`, `Ω=Σ_data`) for comparability with the SMM. Moves Jacobian-at-θ̂_2 and `compute_smm_inference` out of `run_step3` into a new `run_step4` block so inference can rerun without re-optimising; full-column `jacobian_all_step3.npy` is retained for diagnostics. Renames `gb_param_cols`/`gb_param_indices_step3`/`beta_T_start_raw`/`S_`/`R_down_` to `gb_cols`/`gb_param_idx`/`beta_T_start`/`S`/`R_downstream` to match `main.jl`. Adds `param_labels`, `moment_labels`, and `gamma_ref_map` kwargs to all `compute_smm_inference` calls. GMM semantics unchanged: `K=1` Jacobian, `K_sim=0`, `Ω=Σ_data`, `analytical=true`, `gmm_note.txt` preserved. |
| 2026-06-04 | `load_parameters.jl`, `model_CP.jl`, `model_analytical.jl`, `tools.jl`, `pso_integration.jl`, `main.jl`, `main_gmm.jl`, `main_pso.jl`, `CLAUDE.md` | Decouple trade-cost parametrization (`N_TAU`) from reg_coef moment count (`N_REG`). Replaces single `N_beta` with two constants: `N_REG = n_coef` (reg_coef moment count, moment axis) and `N_TAU = n_tau` (β-parameter count, parameter axis). Default `n_tau = n_coef` preserves all existing behavior. Target config `N_TAU=1, N_REG=4`: power-law τ=d^α (one α param) with four binned regression moments (over-identified, df=3 on reg_coef). `build_tau`/`unpack_params`/PSO β-slices key off `N_TAU`; `fast_weighted_regression`/`compute_regression_quadrature`/`distance_bin`/`BLOCK_RANGES[4]`/`Sigma_beta_gamma` file selection key off `N_REG`. Two runtime asserts added in main.jl and main_gmm.jl: (1) Ω size == `N_REG + n_γ`; (2) β/α label count in `gb_cols` == `N_TAU`. New arg: `julia main.jl aero 4 1` → N_REG=4, N_TAU=1. `PARAM_LABELS` β entry is "alpha" when N_TAU==1. |
| 2026-06-02 | `main.jl`, `tools.jl`, `load_parameters.jl`, `CLAUDE.md` | Fix β+γ inference singularity and reconcile gamma_threshold cascade. `main.jl`: inference Jacobian now restricted to β+T COLUMNS (not just β+γ rows) at θ̂_1 and θ̂_2 → G'WG full-rank, exactly identified (df=0); Step-2 Jacobian `base_seed` 0→2_000_000 (decorrelate from Σ_sim draws); deleted duplicate end-of-file Step-2/diagnostic block that overwrote K=50 artifacts with K=2. `tools.jl`: `build_step3_weight_matrix` reconciles `Sigma_beta_gamma` size to the (possibly thresholded) active set with a three-way guard (regenerated / pre-threshold-subset / error), no-op when threshold=0; `compute_jacobian` gains an `all(isfinite)` guard naming the offending (moment,param). `load_parameters.jl`: gamma threshold block moved above `T_mask_local = vec(X_rs_local) .> 0` so pruned pairs are excluded from T_MASK/n_good; `empirical_moments` γ block now sourced from `emp_gamma_ls_local` (thresholded+renormalized, consistent with reporting and Hansen-J); one-upstream-region diagnostic (error on 0 survivors, warn on 1). GMM Σ reordering (γ-first) left as a separate task. |
| 2026-05-28 | `model_analytical.jl` | `compute_regression_quadrature` regressand changed from closest-destination win probability to the whole-industry extensive margin ρ̃_{r's}(z) = 1 − ∏_dr (1 − ρ_{r'dr s}(z)) (Eq. B6), aligning the analytical `reg_coef` with the SMM `linkages_flat`; FWL/WLS design unchanged. |
| 2026-05-28 | `main_gmm.jl` | Fix step-3 GMM inference order-condition failure. `J2_gb` was 251×278 (β+γ rows, all-param cols), making `G'WG` rank-251 against 278 columns → singular, df = −27, meaningless SEs. Fix: restrict Jacobian columns to β+T only (`gb_param_cols = findall(i -> i >= beta_T_start_raw, jacobian_param_indices)`), passing `gb_param_indices_step3` as `param_indices` to `compute_smm_inference`. `jacobian_all_step3.npy` still holds the full-column diagnostic Jacobian. |
| 2026-05-28 | `tools.jl`, `main.jl`, `main_gmm.jl`, `model_CP.jl`, `claude.md` | Make step-3 β+γ-only loss and inference consistent and crash-free. `compute_smm_inference` gains `block_ranges`/`block_names` kwargs (zipped in the residual loop). `loss_function` no longer re-subsets a W already sized to `moment_indices`. `run_pso_optimization` loss block fixed `[3,4]`→`[4,5]` (reg_coef+gamma_ls). `main.jl` step-2/step-3 inference `gb_indices` corrected from γ-then-β to β-then-γ (matching `W_step3`/`Sigma_beta_gamma.npy`); step-3 inference now slices `J2`/emp/sim to the gb rows instead of passing full-length vectors against a gb-sized W. Docs corrected to β-then-γ ordering throughout. |

---

## Architecture

Three-step efficient SMM/GMM estimator with spatial comovement moments.

Files are organized into role-based folders (see `README.md` for the full tree and
each folder's `files.md`). Julia `include()` paths are relative to the *including
file's* directory: root entry points reach into `optimizers/`, `test/`, and
`extras/`; files inside those folders reach back with `../`.

**Root (optimization core):**
- **`main.jl`** — SMM entry point (simulation-based, stochastic moments)
- **`main_gmm.jl`** — GMM entry point (analytical EK moments, `model_analytical.jl`)
- **`model_CP.jl`** — Core model: `SMM`, `full_SMM`, `loss_function`, `BLOCK_RANGES`, `BLOCK_NAMES`
- **`model_analytical.jl`** — Closed-form EK moments + Gauss-Legendre quadrature for `reg_coef`
- **`optimizer.jl`** — Backend-neutral search hub: `optimize_stage`, `train_stage`, `run_optimization` (aliases `train_stage_pso`, `run_pso_optimization`)
- **`tools.jl`** — Inference, Jacobian, weight matrix, reporting
- **`load_parameters.jl`** — Data loading, globals (`BLOCK_RANGES`, `MOMENT_MASK`, `U_DRAWS`, …)

**`optimizers/`** (interchangeable engines behind `--optimizer=`):
- **`pso_integration.jl`** — PSO backend (`parallel_pso_smm`), default
- **`cmaes_integration.jl`** — CMA-ES backend (`parallel_cmaes_smm`)
- **`tiktak_integration.jl`** — TikTak multistart backend (`parallel_tiktak_smm`)

**`documentation/`** — conceptual docs: `model.md` (model + math + Sobol draws), `optimizer.md` (PSO/CMA-ES), `inference.md` (SEs, Jacobian, Hansen J), and the granular / attraction-area set: `finite_sample2.tex` (the model), `plan_granular_aa.md` (the design as implemented), `granular_validation.md` (gates + open assumptions), `granular_aa_changes.md` (the complete record of what changed and why).
**`test/`** — validation & diagnostics (`run_internal_validity.jl`, `run_untargeted_validation.jl`, `test_extensive_margin.jl`, `test_t_reorder.jl`, `run_test.sh`).
**`extras/`** — post-estimation analysis (`untargeted_moments.jl`, `compute_amplification_weights.jl`, `run_amplification_analysis.jl`).

---

## Critical caveats (read before touching inference / Jacobian / T-identification)

**⚠ The analytical/AD Jacobian is FKG-biased on α (and the whole `reg_coef` block) — do
NOT use it for α inference or α identification.** The closed-form `reg_coef` moment
(`compute_regression_quadrature`, and hence `analytical_jacobian_ad` / any
`analytical=true` path) builds the whole-industry extensive margin with the FKG product
approximation `1 − ∏_dr(1 − ρ_dr)` instead of the exact union-of-boxes measure. That
approximation biases the derivative of `reg_coef` w.r.t. the trade-cost parameter α
(and the reg_coef block generally). So: the AD Jacobian is exact for `γ_ls`/shares but
**biased for the α column via reg_coef** — for α standard errors / identification use the
**simulation-based** Jacobian (SMM `main.jl`, `analytical=false`), never the AD/analytical
one. (This is why `main.jl` keeps the loss simulation-based under `profile_T`, and why the
GMM AD path is *not* the reference for α inference.) The AD-vs-FD cross-check agreeing to
`O(δ²)` does **not** clear this — both share the same FKG regressand; the bias is in the
estimand, not the differentiation.

**⚠ `H = J'WJ` restricted to the T parameters is block-diagonal by sector — read it PER
SECTOR (the diagonal `H[T_s, T_s]` blocks), not as one global `H[T,T]`.** Each sector's T
columns only enter that sector's `γ_ls` moments (the `M^s` structure), so the true T
identification lives in the per-sector diagonal blocks. The global `H[T,T]` λ_min /
condition number mixes sectors and is dominated by cross-sector off-diagonal coupling
that is a **weighting/units artifact** (e.g. the `screen_T_identification` output showing a
λ_min eigenvector concentrated ~100% on one sector, or a huge cross-sector "coherence"):
that is expected and does not mean T is globally unidentified. Judge T identification from
the **per-sector `H[T_s,T_s]` (or `M^s`) spectra**, sector by sector — a well-conditioned
per-sector block means that sector's T is identified regardless of the global `H[T,T]`
condition number.

---

## Moment blocks

Moments are ordered as five blocks in the masked vector (`MOMENT_MASK` applied):

| Index | Name | Description |
|-------|------|-------------|
| 1 | `agg_labor_share` | Aggregate labor share |
| 2 | `agg_industry_share` | Industry shares π_s |
| 3 | `pi_r` | Regional market shares |
| 4 | `reg_coef` | Distance-bin regression coefficients (`N_REG` entries); GMM (quadrature): regressand is the extensive margin ρ̃_{r's}(z) = 1 − ∏_dr (1 − ρ_{r'dr s}(z)) (Eq. B6, whole-industry), regressors/FE keyed on nearest downstream region. `N_REG` is the moment count (= `n_coef` arg); independent of `N_TAU`. |
| 5 | `gamma_ls` | Location-specific linkage shares γ_ls. Under `CA_LEVEL == :aa` these are the ATTRACTION-AREA aggregates `γ_{s,a} = Σ_{l∈a} γ_{ls}`, summed over every cell of the area (control cells included, on both the data and model side). |
| 6 | `G0` | **`GRANULAR` only.** The count moment `Ḡ_s(0) = mean_l (1−q̂_ls)^{N̂_s}` — the share of cells inside active attraction areas hosting zero suppliers, `S` entries. Evaluated in CLOSED FORM (it is the exact expectation of the realised share), so it is unbiased and noise-free; nothing is simulated on a finite economy. **Appended, never inserted**, so every existing index into blocks 1–5 is untouched. |

`BLOCK_RANGES` is a 5-tuple of index ranges into the masked vector, one per block —
a 6-tuple under `GRANULAR`. Assemble the masked moment vector with
`moments_to_vec(sim)`, never a hard-coded `vcat([vec(m[i]) for i in 1:5]...)[MOMENT_MASK]`,
which would silently drop block 6.

---

## Invariant: β+γ subsystem ordering

**β (reg_coef) first, then γ (gamma_ls), then G (`GRANULAR` only):**

```julia
gb_indices = inference_moment_indices()   # BLOCK_RANGES[4], [5], and [6] under GRANULAR
```

This ordering applies everywhere: the Σ files on disk, `W_step3`, `Omega`, and `gb_indices`
in `main.jl` and `main_gmm.jl`. Do not reverse it. Block 6 is appended, so the β and γ
positions are exactly what they were. Use `inference_moment_indices()` /
`inference_block_layout()` rather than open-coding the `vcat`.

---

## Invariant: T flat-indexing convention (s-major)

**Both Jacobian axes share one flattening: s-major (region-minor).** The γ-moment
rows and the T-parameter columns enumerate `(s,r)` pairs in the *same* order — all
regions of sector 1, then all regions of sector 2, … — so the `T[s,r]` column aligns
with the `γ[s,r]` row.

```julia
T_MASK = vec(permutedims(T_ACTIVE))      # == T_MASK_MOMENT (s-major over (S, T_COL_DIM))
```

**Two index spaces.** `T_COL_DIM` is the width of the T-parameter/γ-moment column space:
upstream ZE under `CA_LEVEL == :ze` (so `T_ACTIVE == active_mat` and everything below reads
exactly as before, with `T_COL_DIM == R`), attraction areas under `:aa` (`T_ACTIVE ==
AA_ACTIVE`, `T_COL_DIM == n_AA`). The *cells* actually simulated are a separate set,
`CELL_MASK :: BitMatrix(S, R)` — under `:aa` it includes the control cells, which are goods
there. Never index one space with the other: use `T_COL_DIM`/`SECTOR_T_COLS`/`EMP_GAMMA_T`
for T and γ, and `n_good`/`GOOD_S`/`GOOD_R`/`SECTOR_GOOD_*` for cells. `T_GATHER[l]` maps a
ZE to its T column (the identity under `:ze`).

- **Flat position** of active pair `(s,c)` in the reduced-T / moment axis:
  `flat_pos = (s-1)*T_COL_DIM + c`. Inverse: `s = (flat_pos-1)÷T_COL_DIM + 1`,
  `c = (flat_pos-1)%T_COL_DIM + 1`.
- **`unpack_params`** inverts this flatten: `T_full = zeros(T_COL_DIM*S); T_full[T_MASK] =
  T_reduced; T_par = permutedims(reshape(T_full, T_COL_DIM, S))`, ref-normalises per sector,
  then **gathers** onto ZE: `T[s,l] = T_par[s, T_GATHER[l]]`. It still **returns region-major
  `vec(T_mat)`** at the (S, R) ZE level, so the inter-stage full-T representation is unchanged.
  `unpack_T_par(params)` returns the `(S, T_COL_DIM)` parameter matrix *before* the gather —
  that is what the Sinkhorn inversion, the optimizer's warm-start reduction and the T
  delta-method iterate on.
- **Every reduced-T producer must emit s-major**: PSO init/bounds use
  `vec(permutedims(T_rs_init))[T_MASK]`; the warm-start reduce uses
  `vec(permutedims(reshape(params_dict[:T], S, R)))[T_MASK]`; `build_step3_weight_matrix`'s
  Σ_data subset uses `collect(T_MASK)` directly (T_MASK is already the moment convention).
- **`main_pso.jl`** shares `model_CP.unpack_params`, so its own `T_MASK`/`good_indices`/`T_init`
  are flipped to s-major in lockstep. A partial application silently transposes `T_mat`
  (runs fine, fits wrong T) — see `test_t_reorder.jl`.
- **`GOOD_S/GOOD_R` are region-major** (`findall` over the (S,R) mask) and are *not*
  required to match the reduced-T order: the model reads `T_mat[s,r]` via `SR_TO_GOOD`, and
  no code positionally aligns `GOOD[g]` with the reduced-T block.
- The dormant `second_stage` `mask_emp_gamma_ls` path is unaffected by this convention
  (off in all main flows).

---

## Input data layout

| File | Shape | Description |
|------|-------|-------------|
| `filter_N_upstream.npy` | `(S, R)`, values in `{0, 1}` | **BINARY.** `1` ⟺ the (sector, ZE) cell enters the optimisation. Within the kept cells, *supplier* ⟺ `X_rs > 0`, *control* ⟺ `X_rs == 0`. The old three-status encoding (0 / 1 / 2) is gone; `supplier_cells = (filter .== 1) .& (X_rs .> 0)` still selects exactly the supplier cells, so the legacy path needed no edit. Under `:aa` that matrix no longer defines the estimated set — it defines `𝒜⁺` — while `CELL_MASK = filter .== 1` does. |
| `attraction_area_linkages.npy` | `(R, R_downstream)` binary | ZE → attraction-area incidence, each area anchored on the ZE's closest downstream region. Required under `--ca_level=aa`. Asserted at load: exactly one area per ZE, and `argmax_col == CLOSEST_DOWNSTREAM_REGION` — the model's fixed effect and the empirical `A129_AA` grouping must be the SAME partition. |
| `G_K.csv` | `A129`, `K`, `G(K)`, `N_supplier_s` | `G(K) = Pr(K_ls ≤ K)`; `G(0)` is the share of ZE with zero suppliers, denominator = ZE inside active attraction areas. `N_supplier_s` (constant within `A129`, asserted) gives the variety-count bounds `N_LO = ⌈N_supplier_s / R_downstream⌉`, `N_HI = N_supplier_s`. Required under `--granular=true`. |
| `Sigma_beta_gamma*.npy` | `(N_REG + n_γ + S)²` | Joint bootstrap covariance, **three blocks in the order β → γ → G** (`BLOCK_RANGES[4]`, `[5]`, `[6]`). β-block dimension is `N_REG` (moment count), independent of `N_TAU`; the `G` block is `S` rows of `Ḡ_s(0)`. Legacy mode slices to the leading `N_REG + n_γ`; granular keeps all three. Eight variants, selected by `sigma_beta_gamma_filename(; smm, aa)`: `Sigma_aa_` PREFIX under `--ca_level=aa`, `_cloglog` under `REG_METHOD == :cloglog`, `_1` when `N_REG == 1`, trailing `_f` on the SMM path. |
| `w_gamma.npy` | `(n_gamma_kept, n_gamma_kept)` | Bootstrap covariance of γ_ls moments (fallback) |
| `w_beta.npy` | `(N_REG, N_REG)` | Bootstrap covariance of β moments (fallback) |

---

## Key functions

### `tools.jl` — `build_step3_weight_matrix`

Assembles the efficient SMM weight matrix `W_step3 = (Σ_data + Σ_sim)^{-1}` over
β and γ moments only.

- File selection keyed on `N_REG` (moment count): `Sigma_beta_gamma_1.npy` for `N_REG==1`, `Sigma_beta_gamma.npy` for `N_REG∈{4,5}`. Independent of `N_TAU`.
- Σ_data is the joint bootstrap covariance of reg_coef and γ_ls moments, **ordering: β block first, then γ block** (`BLOCK_RANGES[4]` followed by `BLOCK_RANGES[5]`). β-block dimension = `N_REG`.
- Estimates Σ_sim from K re-seeded `full_SMM` evaluations at `theta_hat_1`, restricted to `gb_indices` (β then γ).
- Returns `W_step3` of size `(N_REG + n_gamma_kept) × (N_REG + n_gamma_kept)`.
- Assert: `size(Sigma_data,1) == N_REG + count(survive)` (not `N_TAU`).

### `tools.jl` — `compute_smm_inference`

Computes parameter SEs (efficient + sandwich), fitted-moment SEs, residual SEs, and Hansen J-test.

**Signature:**
```julia
compute_smm_inference(theta_hat, J, W, Omega;
    param_indices, empirical_moments_vec, simulated_moments_vec,
    output_folder, industry, K_sim,
    block_ranges = BLOCK_RANGES,   # restricted subsystem or global default
    block_names  = BLOCK_NAMES)
```

The `block_ranges`/`block_names` kwargs control per-block diagnostics in the residual SE loop.
When called for the β+γ subsystem, pass:
```julia
n_reg_loc = length(BLOCK_RANGES[4]); n_gam_loc = length(BLOCK_RANGES[5])
gb_block_ranges = (1:n_reg_loc, (n_reg_loc+1):(n_reg_loc+n_gam_loc))
gb_block_names  = ("reg_coef", "gamma_ls")
```
Defaults (`BLOCK_RANGES`/`BLOCK_NAMES`) leave full-vector callers unaffected.

### `tools.jl` — `run_pso_optimization`

Unified PSO wrapper for Steps 1 and 3.

- `moments_loss_gamma_beta=true` → `moment_blocks=[4,5]` (reg_coef + gamma_ls). **Note:** blocks 4 and 5 are `reg_coef` and `gamma_ls` respectively; blocks 3 and 4 would be `pi_r` + `reg_coef` (incorrect for β+γ).
- `gamma_beta_only=true` → optimises only `beta` and `T` parameters (A_r/labor/industry fixed at warm-start).

### `model_CP.jl` — `loss_function`

Computes `err * W * err'` with optional moment subsetting via `moment_indices`.

W-size collision guard: if `W` is already restricted to `length(moment_indices)` rows/cols
(e.g. a pre-built `W_step3` passed alongside `moment_blocks`), it is used as-is.
A full-size `W` is subsetted as before. This prevents `BoundsError` when step-3 loss runs.

---

## Step-3 inference scope

Inference at θ̂_2 (`main.jl` and `main_gmm.jl`) is computed on the **β+γ moments only**,
ordered β then γ:

```julia
gb_indices = vcat(collect(BLOCK_RANGES[4]), collect(BLOCK_RANGES[5]))
J2_gb      = J2[gb_indices, :]
sim_vec_gb = sim_vec_2[gb_indices]
emp_vec_gb = emp_vec[gb_indices]
```

The full-row Jacobian (`J2`, all moment rows) is still computed and saved to
`jacobian_all_step3.npy` for diagnostics; only the inference slice is gb-restricted.

The step-3 PSO loss is likewise restricted to β+γ via `moment_blocks=[4,5]`.

`compute_smm_inference` takes `block_ranges`/`block_names` kwargs so per-block diagnostics
index the restricted vector correctly. `loss_function` leaves a pre-restricted W
(size == `length(moment_indices)`, e.g. `W_step3`) un-subset.

### T-identification eigen-screen

`screen_T_identification` (in `tools.jl`) runs **after every Jacobian computation**, at
both θ̂_1 (Step 2) and θ̂_2 (Step 4), in `main.jl` and `main_gmm.jl`. It is fed the same
gb slice used for inference (`J_gb`/`J1_gb`/`J2_gb` and the matching gb weight matrix
— `Weight_matrix_custom[gb,gb]`, `W_step3`, `W_step1`, or `W_eff`) plus
`param_labels=PARAM_LABELS[gb_cols]`. It prints (label-prefixed): the global
`H = J'WJ` eigen-screen (λ_min/λ_max/cond/rank), the T-only sub-block `H[T_cols,T_cols]`
eigenvalues, and the per-sector analytical screen. It is **print-only** — no estimate,
weight matrix, inference output, or file is affected. The historical `main.jl`
duplicate-`step_rel`-kwarg bug in the θ̂_2 Jacobian call was fixed at the same time.

**Mechanism attribution** (when the `H[T,T]` sub-block is non-empty): three further
diagnostics attribute a small `H[T,T]` λ_min to one of three mechanisms. (1) The λ_min
eigenvector `v_min` of `H[T,T]` is mapped to sectors by parsing each T column's
`"T[sname-rname]"` label (split on the LAST '-'); per sector the share of squared mass
`Σ v_min[sector]²/Σ v_min²` and dominant sign are printed, flagging CONCENTRATED (one
sector > 0.8 mass → **mechanism 3**, level-weak sector) vs SPREAD with mixed signs
(→ **mechanism 1**, inter-sectoral flat direction). (3) `H_I = J'J` (identity weighting)
and its `H_I[T,T]` λ_min/conditioning are printed beside the W-weighted values: weak
under W_eff but fine under I ⇒ **mechanism 2** (W de-identifies the T-via-γ_ls params);
weak under both ⇒ intrinsic to J. (2) After the per-sector analytical screen, a
cross-check prints each sector's v_min support/sign beside its M^s ratio λ_min/λ_max,
matching the worst-M^s sector to the support of the global weak direction. All three are
**print-only**. `global_eigen` gains additive fields `T_evec_support`, `T_evec_signs`,
`T_evec_sectors`, `T_eval_min_identity` (existing fields and call sites unchanged — call
sites discard the return value).

---

## Two-constant parameter layout

`N_REG` and `N_TAU` are independent constants set in `load_parameters.jl`:

| Constant | Meaning | Axis | Set by | Used in |
|----------|---------|------|--------|---------|
| `N_REG` | reg_coef moment count | moment | `n_coef` arg | `fast_weighted_regression`, `compute_regression_quadrature`, `distance_bin`, `BLOCK_RANGES[4]`, Σ_data file selection |
| `N_TAU` | trade-cost parameter count | parameter | `n_tau` arg (default = `n_coef`) | `build_tau`, `unpack_params`, PSO β-slice, `PARAM_LABELS` β section, `get_param_start_index(:T)` |

### Granular / attraction-area constants

| Constant | Meaning |
|----------|---------|
| `GRANULAR` | finite `N_s` + the count moment (block 6). `false` ⇒ the continuum model, 5-block moment vector. Requires `CA_LEVEL == :aa` |
| `CA_LEVEL` | `:ze` or `:aa` — the level of comparative advantage AND of the γ block |
| `T_COL_DIM`, `T_ACTIVE`, `T_GATHER`, `SECTOR_T_COLS` | the T-parameter / γ-moment column space (see the invariant above) |
| `CELL_MASK`, `CELLS_OF_SECTOR` | the simulated (sector, ZE) cells — a DIFFERENT set from `T_ACTIVE` under `:aa` |
| `AA_OF_ZE`, `n_AA`, `AA_ACTIVE` | the attraction-area map and the per-sector active set `𝒜⁺_s` |
| `EMP_GAMMA_T`, `EMP_GAMMA_T_TILDE` | the block-5 target and the Sinkhorn row margin, in T-column space |
| `N_LO`, `N_HI`, `G_TARGET` | variety-count bounds and the `Ḡ_s(0)` target, from `G_K.csv` |
| `N_rho` | draws per (sector, cell), in both modes. Under `GRANULAR` it also sets the precision of `q̂` hence of `N̂_s` |
| `REG_INCLUDE_CONTROL`, `REG_INCLUDE_SIZE` | the effective regression design: under `:aa`, control rows OFF (those cells are goods) and the log-z size control ON |

`--granular=true` **requires** `--ca_level=aa` (hard error otherwise): under ZE-level
comparative advantage only the supplier cells are estimated, so no cell can come out empty —
the count moment has no content and `T > 0` would be fitted for regions whose observed
`γ_ls` is 0. `(false, :aa)` — the AA-level continuum — is legal and only warns. `(false, :ze)`
is the model as it stood.

Standard runs: `n_tau` unset → `N_TAU = N_REG = n_coef` (no behavior change).
Over-identified run: `n_tau=1, n_coef=4` → `N_TAU=1` (power-law α), `N_REG=4` (four binned moments), df=3 on reg_coef block.

---

## Usage

```bash
# Granular varieties + attraction-area comparative advantage (documentation/plan_granular_aa.md)
./run.sh aero --n_coef=4 --n_tau=1 --granular=false --ca_level=ze   # the legacy model (V0 reference)
./run.sh aero --n_coef=4 --n_tau=1 --granular=true  --ca_level=aa
julia test/test_granular_aa.jl aero 4 1 true aa                    # the validation gates

# SMM (simulation-based)
julia main.jl auto 1        # N_REG=1, N_TAU=1 (default: n_tau=n_coef)
julia main.jl aero 4        # N_REG=4, N_TAU=4 (bin parametrization, exactly identified on reg_coef)
julia main.jl aero 4 1      # N_REG=4, N_TAU=1 (power-law τ=d^α, over-identified df=3)

# Analytical GMM
julia main_gmm.jl aero 4          # N_REG=4, N_TAU=4, n_quad=200
julia main_gmm.jl aero 4 1        # N_REG=4, N_TAU=1 (over-identified)
julia main_gmm.jl aero 4 1 200 500  # N_REG=4, N_TAU=1, n_quad=500
```
